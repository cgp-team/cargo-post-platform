<!-- 我的页 - 个人中心 + 卖家入口 -->
<view class="page-mine {{elderlyMode ? 'elderly-mode' : ''}}" style="{{themeStyle}}">

  <!-- 未登录态 -->
  <view wx:if="{{!isLoggedIn}}" class="login-banner">
    <view class="login-safe" style="height: {{statusBarHeight || 0}}px"></view>
    <view class="login-content" bindtap="goToLogin" hover-class="hover">
      <text class="login-avatar">👤</text>
      <text class="login-text">点击登录，享受更多服务</text>
      <text class="login-arrow">›</text>
    </view>
  </view>

  <!-- 已登录态 -->
  <view wx:else>
    <!-- 用户信息区 -->
    <view class="user-banner">
      <view class="banner-safe" style="height: {{statusBarHeight || 0}}px"></view>
      <view class="banner-bg"></view>
      <view class="banner-content" bindtap="goToProfile" hover-class="hover">
        <view class="user-avatar-wrap">
          <image wx:if="{{userInfo.avatar}}" class="user-avatar-img" src="{{userInfo.avatar}}" mode="aspectFill" />
          <text wx:else class="user-avatar-emoji">👤</text>
        </view>
        <view class="user-detail">
          <text class="user-name">{{userInfo.nickname || '用户'}}</text>
          <text class="user-mobile">{{userInfo.mobile || ''}}</text>
        </view>
        <view class="user-role-tag">🏘 村民</view>
      </view>
    </view>

    <!-- 卖家核心入口（适老化大按钮） -->
    <view class="farmer-section">
      <view class="farmer-title">🧑‍🌾 农户服务</view>
      <view class="farmer-buttons">
        <view class="farmer-btn send-btn" bindtap="goToSend" hover-class="hover">
          <text class="farmer-btn-emoji">📮</text>
          <text class="farmer-btn-text">我要寄货</text>
          <text class="farmer-btn-desc">语音下单 · 大巴承运 · 直达城里</text>
        </view>
        <view class="farmer-btn ledger-btn" bindtap="goToLedger" hover-class="hover">
          <text class="farmer-btn-emoji">📒</text>
          <text class="farmer-btn-text">我的账本</text>
          <text class="farmer-btn-desc">查看收入 · 寄货记录</text>
        </view>
      </view>
    </view>

    <!-- 功能菜单 -->
    <view class="menu-section">
      <view class="menu-item" bindtap="goToOrders" hover-class="hover">
        <text class="menu-emoji">📋</text>
        <text class="menu-text">我的订单</text>
        <text class="menu-arrow">›</text>
      </view>
      <view class="menu-item" bindtap="goToMySend" hover-class="hover">
        <text class="menu-emoji">📮</text>
        <text class="menu-text">我的寄货</text>
        <text class="menu-arrow">›</text>
      </view>
      <view class="menu-item" bindtap="goToBusFavorites" hover-class="hover">
        <text class="menu-emoji">🚌</text>
        <text class="menu-text">公交收藏</text>
        <text class="menu-arrow">›</text>
      </view>
      <view class="menu-item" bindtap="switchToDriver" hover-class="hover">
        <text class="menu-emoji">🚐</text>
        <text class="menu-text">切换为司机模式</text>
        <text class="menu-arrow">›</text>
      </view>
    </view>

    <!-- 设置区 -->
    <view class="menu-section">
      <view class="menu-item" bindtap="goToAddress" hover-class="hover">
        <text class="menu-emoji">📍</text>
        <text class="menu-text">收货地址</text>
        <text class="menu-arrow">›</text>
      </view>
      <view class="menu-item" bindtap="goToProfile" hover-class="hover">
        <text class="menu-emoji">👤</text>
        <text class="menu-text">个人资料</text>
        <text class="menu-arrow">›</text>
      </view>
      <view class="menu-item" bindtap="toggleElderly" hover-class="hover">
        <text class="menu-emoji">🔍</text>
        <view class="menu-item-body">
          <text class="menu-text">老年人模式</text>
          <text class="menu-sub">更大字体，方便长辈使用</text>
        </view>
        <view class="toggle-switch {{elderlyMode ? 'on' : 'off'}}">
          <view class="toggle-knob"></view>
        </view>
      </view>
      <view class="menu-divider"></view>
      <view class="menu-label">🎨 主题颜色</view>
      <view class="theme-row">
        <view
          wx:for="{{themeList}}"
          wx:key="key"
          class="theme-chip {{themeColor === item.key ? 'active' : ''}}"
          style="background: {{themeColor === item.key ? item.color : '#F5F5F5'}}; color: {{themeColor === item.key ? '#fff' : '#666'}}"
          data-key="{{item.key}}"
          bindtap="switchTheme" hover-class="hover"
        >{{item.name}}</view>
      </view>
    </view>

    <view class="menu-section">
      <view class="menu-item logout" bindtap="handleLogout" hover-class="hover">
        <text class="menu-emoji">🚪</text>
        <text class="menu-text">退出登录</text>
        <text class="menu-arrow">›</text>
      </view>
    </view>
  </view>

  <!-- 底部安全区 -->
  <view class="safe-area-bottom"></view>
</view>
