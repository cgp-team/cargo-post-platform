## 背景

对小程序进行一次全面的界面（WXML/WXSS）与交互体验审查后，集中修复 18 项问题（高 6 / 中 7 / 低 5+），共 46 个文件。全部改动已用 `node --check` 通过语法校验。

## 高优先级

- **司机端主题/适老化断链（H1）**：workbench / routes / earnings 三页根节点补绑 `elderly-mode` 与 `themeStyle`，并新增 30 条 `.elderly-mode` 字号放大规则
- **图标硬编码主题色（H2）**：`appearance.apply` 新增输出 `iconColor / iconDeep / iconAccent / iconClay`；8 个 WXML 共 20+ 处硬编码色改为绑定，图标随主题切换（白/灰/红等语义固定色保留）
- **商城分类假过滤（H3）**：`switchCategory` 按商品名关键词真正过滤列表（后端暂无分类字段，后续接 `categoryId` 后替换）
- **goods 页 `goToProfile` 未定义（H4）**：补上，跳「我的」
- **首页下拉刷新同步 stop（H5）**：改为 `Promise.all(...).finally(stop)`，等数据回来再收起动画
- **司机 TabBar 无交互反馈（H6）**：补 hover 按压态 + 触感反馈；`reLaunch → redirectTo` 消除切页重建闪烁；新增 `elderly` 属性

## 中优先级

- **M1** custom-tab-bar 适老化：字号 22→30rpx、图标 44→52
- **M2** 触感反馈补齐：下单成功、取消订单、主题/老年模式切换
- **M3** bus 首页/详情错误态可点击重试 + 重试按钮
- **M4** 删除无入口的 register 死页面（4 文件 + app.json 注册项）
- **M5** 新增 `utils/auth.js` 统一 `requireLogin()` 登录拦截，替换三处各自为政的写法；`api.js` 401 加 2s 防抖，避免并发失效时重复跳转/叠加 toast
- **M6** parcel / bus 适老化规则补齐（乘车安排、承运信息、审核提示、线路副标题、空态等）
- **M7** 下单成功 `navigateTo → redirectTo`，消除返回栈混乱

## 性能与代码质量

- **L1** workbench 删除 `updateComputedStyles` 二次 setData，样式串改 WXML 直接绑定（省 7 处调用）
- **L2** bus 定时刷新加轨迹签名：轨迹未变只更新车辆位置，不再每 15s 重设地图中心（顺带修复地图跳动）
- **L3** `appearance.apply` 加 diff，值未变不 setData
- **L4** 商品图加 `lazy-load`
- **L5** 村庄列表抽为 `util.VILLAGES`（3 页共用）；重复 4 份的 `formatTime` 抽为 `util.formatBackendTime`
- **L6** TabBar `wx:key` 改 `pagePath`

## 回归建议

1. 司机端三页开老年模式验证字号；
2. 切换四套主题后检查各页图标颜色跟随；
3. 商城分类切换过滤、首页下拉刷新动画；
4. 公交页拖动地图后等 15s，确认不再跳回中心；
5. 未登录点「我要寄货/下单」弹登录确认框；
6. 司机端 tab 间切换（redirectTo 后返回手势行为变化，请真机确认）。

## 说明

- `miniprogram/project.config.json` 的 appid 为本地开发者工具自动改写，未包含在本 PR。
