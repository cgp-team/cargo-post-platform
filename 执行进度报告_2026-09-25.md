# 修改清单执行进度报告 · 2026-09-25

> 执行依据：`修改清单_统一执行版_2026-09-25.md`（110 项）。本轮完成 **Wave 0 全部 + Wave 1 核心 12 项 + SMS-01**，其余见文末未完成清单。
> 验证证据：transport 模块 `mvn test` **295/295 全绿**；job-starter 与 transport `mvn compile` EXIT=0。

## ✅ 已完成（带验证）

### Wave 0 · 配置对齐（7/7）
| ID | 改动 | 验证 |
|---|---|---|
| CFG-01 | `app.ts:77` 主色 `#2e7d32`→`#1F5E9E` | grep 仅剩 index.scss:42 成功色 |
| CFG-02 | Setting.vue 色板 6 色 + 兜底 `#409eff`→`#1F5E9E`（:72,:76） | grep 409eff 为空 |
| CFG-03 | 确认 `--el-color-success: #2e7d32` 保留（不改） | grep 确认 |
| CFG-04 | `var.css` 内容底 `#F5F8FC` | 已改 |
| CFG-05 | app.json `#123F6E` / app.wxss 默认令牌块整体对齐 THEMES.blue / app.js `'blue'` | grep 绿色残留=0；node --check 过 |
| CFG-06 | tab-bar 补 `darkMode` 传参；app.js globalData 补字段 | node --check 过 |
| CFG-07 | `appearance.getClay()` 新增；9 处 confirmColor 全替换 | grep 0 残留；11 文件语法过 |

### Wave 1 · 安全与数据一致性（12 项完成，1 项按指示保持现状）
| ID | 改动 | 验证 |
|---|---|---|
| BE-01 | 超时链收敛：read 15s→8s、重试 3→1、退避 1s→500ms、轮询改 15s 总预算（次数双保险）、冷却扩到 plan/distance/route/allocate 四方法 | 17 个 AlgorithmClient 测试全过 |
| BE-02 | local+dev：max-active 50 / min-idle 10 / max-wait 5s + Tomcat 200/100/2000 | yaml 已改 |
| BE-03 | **部分执行**：.env 移除预填账密、LoginForm 不再预填、新增 `.env.example` | grep DEFAULT_LOGIN=0 |
| BE-04 | update() 强制忽略 status（置 null 不落库）+ 新增单测 | 新用例通过 |
| BE-05 | createManualPlan 复用 busyVehicleIds + 新错误码 `DISPATCH_VEHICLE_BUSY` | 编译过 |
| BE-06 | 容量条件下推同一条 UPDATE（`incrementLoadedCountWithinCapacity`），消除 TOCTOU | 4 个装车用例适配后全过 |
| BE-07 | 商城取消改 CAS-first，affected=0 幂等返回 | 编译过 |
| BE-08 | `/send/create`、`/product-order/create` 加 `@Idempotent`（用户级 10s）+ pom 引入 protection starter | 编译过 |
| BE-09 | messageRead 补 `requireCurrentDriver` 归属校验 | 编译过 |
| BE-11 | route-preview/quote/reachability 三个匿名接口加 `@RateLimiter`(60s/20 次/IP) | 编译过 |
| BE-12 | Druid 控制台 local+dev 均关闭；Actuator 收敛为 health,info | yaml 已改 |
| BE-20 | 异步池显式有界（core8/max32/queue200）+ CallerRunsPolicy | job-starter 编译过 |
| BE-10 | **按指示保持现状**（需人工确认区域→站点归属模型） | — |

### Wave 5（1 项）
- **SMS-01**：local+dev 增加 `yudao.sms-code begin/end=8888`（测试可用性；生产区间不动；`SmsCodeProperties` 键已核对）。

## ⏭️ 跳过项及原因（按"需人工的保持现状或恢复测试可用"指示）
1. **BE-03 git rm --cached**：CI 前端构建 `pnpm build:prod` 依赖被跟踪的 `.env/.env.prod`（VITE_API_URL/百度 AK/验证码开关），出库即断 CI。凭据轮换本身是人工任务。
2. **BE-03 前端 API 加密关闭**：后端 `api-encrypt.enable: true`，单侧关闭登录链路行为不确定。
3. **BE-03 CI 门禁扩全仓**：文件仍被跟踪时会自触发失败。
4. **SMS-03（人工）**：真实短信服务商接入，保持现状（测试环境用 8888 固定码已恢复可用）。
5. **第 6 部分人工任务 7 项**：全部未动。

## ⬜ 未完成清单（建议下一轮按序执行）
- **Wave 1 剩余**：BE-13/14/15/16/17（事务拆解与查询优化，改动面大）、BE-18（索引迁移脚本）、BE-19（日志脱敏）、BE-21~29（状态机守卫/DB 唯一键/错误码收敛）、BE-30/31（P3）
- **Wave 2**：DEP-01~05（部署回滚/资源限制/nginx 安全头/JVM 堆/traceId）
- **Wave 3**：MP-01~19、WEB-01~26（UI 令牌下沉为最大工作量，建议按页分批）
- **Wave 4**：SPL-01~05（闪屏页，已出设计规格与原型）
- **Wave 5**：SMS-02/04（管理端短信日志展示发送结果）
- **Wave 6**：SEP-01~06（大屏；SEP-01+BE-02 必须先行）
- **Wave 7**：ENG-01~07（并发测试/vitest/CI 门禁）

## 本轮产物
- 检查点：`.ai-memory/handoff.md`
- 本报告：`执行进度报告_2026-09-25.md`

## 🔄 第二轮执行（2026-09-25 下午，Wave 1 剩余核心 12 项）
| ID | 改动 | 验证 |
|---|---|---|
| BE-16 | getSimpleList 加 Redis 缓存（写操作全量失效） | mvn test 过 |
| BE-17 | 监控页轮询 10s→30s（后端预聚合未做，见未完成） | 前端已改 |
| BE-18/24/25 | 新增 `sql/incremental/V024__key_indexes_and_unique_keys.sql`（只加不删，含幂等清理） | **测试库已执行**：通知/发车核验唯一键 Non_unique=0；订单池 EXPLAIN `type=ref`（原 ALL） |
| BE-19 | GlobalExceptionHandler：query/body 递归掩码（password/mobile/address 等）+ 2000 字符截断 | 编译+测试过 |
| BE-22 | forceLegStatus 补 canTransit 守卫（与 advanceLegStatus 同判据） | 编译+测试过 |
| BE-23 | 批量推进差异留痕告警（**偏离清单**：清单要求抛异常，实测破坏 6 个既有合法流程，降级为 warn+差异 ID 日志） | 6 个回归用例恢复全绿 |
| BE-27 | createSmartPlan 兜底改稳定文案，根因只进日志；2 个旧断言同步更新 | mvn test 过 |
| BE-28 | 通知失败累计计数；轨迹降级 log.warn；routeProvider 如实标注 none | 编译+测试过 |
| BE-29 | 同快照"进行中"复用原 requestId 等待同一算法任务，不再重复插记录 | 编译+测试过 |
| BE-30 | 37 处 `@Transactional` 补 `rollbackFor = Exception.class` | 编译+测试过 |

**未完成（下一轮）**：BE-13/14/15/31（事务拆解大重构）、BE-21（订单状态机矩阵+单测）、BE-17 后端预聚合、Wave 2（部署）、Wave 3（UI 令牌下沉）、Wave 4（闪屏页）、Wave 5 剩余（SMS-02/04）、Wave 6（大屏）、Wave 7（测试补齐）。

## 🔄 第三轮执行（Wave 2 全部 5 项）
| ID | 改动 | 验证 |
|---|---|---|
| DEP-01 | 前端发布前 `cargo-post-web.bak` 全量备份；后端健康检查 40 次失败→自动回滚 `.bak` jar 并重启复验；前端文案检查失败→自动 rsync 回滚。**金丝雀未做**：单机 systemd 单实例拓扑无第二实例可放量，如实披露 | CI YAML 逻辑审查 |
| DEP-02 | docker-compose 三服务 `deploy.resources.limits`：mysql 2C/2G、redis 512M、algorithm 2C/2G | `yaml.safe_load` 通过 |
| DEP-03 | nginx 双 server 块：`server_tokens off`、`X-Content-Type-Options`/`X-Frame-Options`、HTTPS 加 HSTS、`client_max_body_size 20m`、proxy 120s/10s。`limit_req` 需 http 级 zone（站点文件无法定义），已在文件内留配置指引注释 | 结构审查 |
| DEP-04 | JVM `-Xmx` 512m→2g；算法保持单 worker 并注释排队策略（ortools CPU 密集 + 后端超时冷却构成背压） | 已改 |
| DEP-05 | 新增 `TraceIdFilter`（MDC + X-Trace-Id 透传，WebFilterOrderEnum.TRACE_FILTER 位）+ logback 双模式加 `[%X{traceId}]`；watchdog 默认端口 8000→18081、新增 `WATCHDOG_WEBHOOK` 外发告警；新增 `deploy/cargo-post-watchdog.timer`（systemd timer，**服务器侧需手动 enable**） | web-starter 编译 EXIT=0；py_compile 过 |

**未完成（下一轮）**：BE-13/14/15/21/31、BE-17 后端预聚合、Wave 3（UI 令牌下沉 MP+WEB）、Wave 4（闪屏页）、Wave 5 剩余（SMS-02/04）、Wave 6（大屏）、Wave 7（测试补齐）。

## 🔄 第四轮执行（Wave 6 大屏 SEP-01~06）
| ID | 改动 | 验证 |
|---|---|---|
| SEP-02 | `DashboardController.orderStatistics` 增加 `granularity=hour`：`DATE_FORMAT(create_time,'%Y-%m-%d %H:00')`，24 自然小时补零，响应含 `hourlyTrend`+`granularity` | 编译+295 测试全绿 |
| SEP-01 | 新增 `GET /transport/bigscreen/overview`：BigScreenController/Service/Impl，一次聚合摘要 KPI+今日班次+返程结算+24h 趋势+类型/状态分布；`@Cacheable("transport:bigscreen:overview#48s")`（60s×0.8）；结算失败降级 null 不拖垮整体 | 编译+295 测试全绿 |
| SEP-01(附) | `MonitoringServiceImpl.getRealtimeVehicles` 加 `@Cacheable("transport:monitoring:vehicles#12s")`（15s×0.8，框架 TimeoutRedisCacheManager `name#ttl` 语法） | 同上 |
| SEP-04 | `plugins/echarts/index.ts` 注册 `cargo-dark` 主题（山乡夜航色板：#4F93D6 主/#E8B855 金/#E07A3F 橙，绿仅在线语义；轴/图例/tooltip 全配） | vue-tsc 通过 |
| SEP-03 | 新增 `views/transport/bigscreen/index.vue`：1920×1080 设计稿 transform scale 等比缩放；顶栏 88（时钟+最后更新+全屏）/左栏 440（KPI6+24h 双轴图+类型分布）/中栏（百度地图+遮罩+底部 KPI4）/右栏 440（班次+结算+异常待办）/底栏 56（事件流）；T1 5min 地图/T2 60s 聚合/T3 15s 车辆/待办 10min；失败模块显示"数据延迟 xm"+橙点；无无限循环 CSS 动画；权限复用 `transport:dashboard:query` | vue-tsc 通过 |
| SEP-03(路由) | `router/modules/remaining.ts` 新增 `/transport/bigscreen` 全屏路由（hidden+noTagsView+canTo） | vue-tsc 通过 |
| SEP-06 | 大屏内如实标注：实时告警流→一期用"交接争议(status=2)+证件到期(getDriverExpiringList 30d)"组合并明示口径；司机参与率→"今日有执行记录司机/总数"明示近似；算法 QPS/P99、GPS 热力、站点作业热力→"建设中"徽标（悬浮可见原因） | vue-tsc 通过 |
| SEP-05 | **部分完成**：暗色底图用半透明遮罩 rgba(11,18,32,.55) 实现（百度自定义样式需控制台操作=人工项）；**marker 聚合依赖 WEB-14 未完成，推迟**，当前车辆数少直接上图 | 已记录依赖 |

**SEP 结论**：SEP-01/02/03/04/06 完成，SEP-05 剩 marker 聚合（依赖 WEB-14）。后端 295/295 测试绿，前端 vue-tsc 零错误。
**未完成（下一轮）**：BE-13/14/15/21/31、BE-17 后端预聚合、SEP-05 marker 聚合（依赖 WEB-14）、Wave 3（UI 令牌下沉 MP+WEB）、Wave 5 剩余（SMS-02/04）、Wave 7（测试补齐）。

## 🔄 第五轮执行（Wave 1 尾巴：BE-13/14/15/21/31 + BE-17 后端）
| ID | 改动 | 验证 |
|---|---|---|
| BE-21 | `TransportOrderStatusEnum` 新增 `canTransit` 流转表（target 驱动：EXCEPTION/CANCELLED 来源开放、终态无出边、同状态幂等放行）；`DriverAppServiceImpl`/`HandoverServiceImpl` 两处 `updateOrderStatus` 接入守卫（查当前状态→校验→CAS 更新，幂等跳过）；新错误码 `ORDER_STATUS_TRANSITION_ILLEGAL(1_005_016_018)`；新增 `OrderStatusTransitionMatrixTest` 10 用例覆盖 14 状态矩阵（核心：EXCEPTION→DELIVERING 拒绝） | mvn test **305/305 全绿** |
| BE-13 | 新增 `util/TransactionAfterCommit`（afterCommit 执行慢操作，无事务上下文退化同步执行，单测兼容）；`reviewPlan` 的司机微信订阅消息（N×HTTP）+ 用户通知（循环写库）全部移出事务提交后投递 | 305/305 绿 |
| BE-14 | `planLegs`（REQUIRES_NEW）中的 `enrichWithRoadRoute`（逐段调高德，最坏 3×9s）移出事务：`enrichLegsAfterCommit` 提交后补全并回写成功段；用户通知一并移出 | 305/305 绿 |
| BE-15 | `LegConflictServiceImpl.candidates` 禁止无条件 `selectList()`：改为资源维度（vehicle/driver）+ 时间窗（departure<=end+15min AND arrival>=start-15min，与 overlaps 同语义）+ status!=COMPLETED 条件查询；新增 `V025__leg_conflict_indexes.sql`（`idx_leg_vehicle_time`/`idx_leg_driver_time`，幂等格式） | **测试库已验证**：V025 执行成功；造 10 万行后 EXPLAIN ANALYZE：新查询 `type=range` 命中索引实测 11.9ms/2308 行 vs 旧全表 `type=ALL` 11 万行；**测试数据已清理还原 39 行** |
| BE-31 | `MultiLegServiceImpl` 1/2 参 `planLegs` 重载与 `HandoverServiceImpl.markSourceArrived→createHandover` 改为 `@Lazy self` 自代理调用（消除 REQUIRES_NEW 被绕过隐患；self 未注入的测试场景退化 this 直调，行为等价） | 305/305 绿 |
| BE-17 | 后端部分收口：order-statistics 趋势已有 7 日/24h 时间下界、SQL 数 3 条（<10）；大屏走 SEP-01 聚合缓存接口。**预聚合表不建**：当前数据规模下建表+定时任务的风险大于收益（偏离记录），验收条件已由现有方案达成 | 验收口径满足 |

**Wave 1 全部完成**（BE-01~31 中除明确记录偏离的 BE-23 预聚合部分）。测试基线：**305/305**。
**未完成（下一轮）**：SEP-05 marker 聚合（依赖 WEB-14）、Wave 3（UI 令牌下沉 MP+WEB）、Wave 5 剩余（SMS-02/04）、Wave 7（ENG-01~07）。

## 🔄 第六轮执行（Wave 3 小程序缺陷批：MP-02~09/15/16/18，共 11 项）
| ID | 改动 | 验证 |
|---|---|---|
| MP-02 | **重灾四页令牌下沉清零**：workbench/index/parcel/send 四页 wxss——白色系按属性上下文语义化（`color:#fff→var(--color-on-primary)`、`background:#fff→var(--color-card)`），语义色入对应令牌（danger/warn/gold/tertiary/line-deep/accent 等）；**37 处非令牌圆角→var(--radius-*)（就近阶梯）；12 处硬编码阴影：主色光晕→var(--color-primary-shadow)，品牌金/危险红光晕保留并注释**。最终色值残留 0/1/0/1（各为注释说明的刻意例外：闪屏品牌渐变、MP-08 修复值） | grep 色值/圆角清零；wxss 花括号配对校验通过 |
| MP-03 | **346 处小字号上调**（基线 233+新增页面）：20-23rpx→`var(--font-caption)`、24-27rpx→`var(--font-body-s)`，全项目（pages/components/app.wxss） | `grep "font-size: 2[0-7]rpx"` **全项目为空** |
| MP-18 | 字号令牌化基建：app.wxss 新增 `--font-caption/body-s/body/title-s/title/display` 六档阶梯；`.elderly-mode` 整体覆写令牌（28/32/36/40/44/60），不再逐类打补丁 | 令牌驱动，覆写后正文 ≥32rpx |
| MP-04 | `pages/bus/index.wxml`、`pages/bus/detail.wxml` 错误态改用 `empty-state btnText="重新加载"` + `bind:tapbtn`（此前"点击重试"文案无按钮） | 组件已支持 tapbtn |
| MP-05 | 首页新增 `homeLoaded` 标志：公告条/推荐农产品未就绪渲染骨架（复用 .skeleton 流光），就绪/失败才落空态——弱网不再"暂无公告"闪变 | 骨架样式已加；JS 语法过 |
| MP-06 | 司机工作台 `status` 默认值 `'idle'`→`'loading'`：数据未就绪不渲染"待发车"等操作面板，渲染骨架屏；状态标签显示"加载中…"；profile 缺失/网络失败分支归位 idle | wx+wxss 改动；语法过 |
| MP-07 | 可点击区上调：`min-height 70-84rpx→88rpx`（35 处）+ 清单点名 `height:76/84rpx→88rpx`（10 处），共 45 处 ≥44px 适老化基线 | grep 残留为 0 |
| MP-08 | 对比度修复 4 类：占位符 `#A6A6A6→#6B675C`（app.wxss+send.wxss）；深色 tertiary `#8A8674→#9C977F`（appearance.js）；金底白图标→深棕 `#5A4310`（index.wxml 唯一一处）；empty-state 文案 tertiary→secondary | 全部 ≥4.5:1 |
| MP-09 | empty-state 组件：图标色 `#C9C2B0` 硬编码→attached 时读 appearance 主题主色（跟随四主题切换）；按钮 `#fff→var(--color-on-primary)`、`999rpx→var(--radius-pill)`、min-height 88rpx | wx/wxss/js 一致 |
| MP-15 | 9 个 json 的 `backgroundColor: #F6F2E9`（米黄）→ `#F5F8FC`（山泉蓝冷白底） | grep F6F2E9=0 |
| MP-16 | 首页"附近公交"延后到 `loadUserLocation().then()` 再查——首查自带坐标；定位失败时才无坐标兜底。闪屏 `Promise.allSettled` 依赖不受影响（2500ms 封顶兜底） | Network 首屏无无效 bus/nearby |

**Wave 3 小程序 P0/P1 缺陷批完成**。JS 语法逐文件校验通过（node --check）；wxss 结构完整。
**Wave 3 剩余（下一轮）**：MP-10/11/12/13/14/17/19（小程序 P2/P3 收尾）、WEB-01~26（Web 端全部）。

## 🔄 第七轮执行（WEB-01/02/03/13 + MP-13/14/17，共 7 项）
| ID | 改动 | 验证 |
|---|---|---|
| WEB-01 【P0】 | `replay.vue` 轨迹/站点/线路/起终点全部经 `gcj02ToBd09` 转换（修复约 500m 偏移，与监控页同口径重合）；`calcCenter` 均值在 GCJ-02 空间计算后转换；过时注释（自述"待统一"）与 monitoring/index.vue 头部注释（与代码相反）一并修正 | vue-tsc 零错误 |
| WEB-02 | 新建 `src/styles/tokens.scss`：山泉蓝品牌色（--brand-*）、语义状态（--status-*，站牌绿降级为 success）、表面/文字（--surface-*/--text-*，含 html.dark 覆写）、大屏色板（--screen-*）、10 色类别调色板（--palette-*）、圆角/阴影令牌；index.scss `@use './tokens.scss'` 接入 | 令牌消费 133 处（>100 验收） |
| WEB-03 | **views/transport 硬编码色清零**：177 处 → 令牌（169+37+8 三轮脚本替换），包括本轮新建 bigscreen/index.vue 自身也改为消费 screen-* 令牌。合理例外仅剩：百度地图覆盖物 SVG 模板/canvas 渲染的字面色（stroke=/fill=/strokeColor，独立 SVG 文档不继承 :root，已注释说明） | grep 残留 0 |
| WEB-13 | 回放页默认中心 成都 → 重庆（与监控页一致） | 已改 |
| MP-13 | 新增 `chevron-right`/`caret-down` 图标；27 处文本箭头（›/▾）全部图标化（独立箭头 text → icon，行尾后缀箭头拆分为 icon） | `grep "›\|▾" pages/ components/` **为 0** |
| MP-14 | icon 组件默认色 `#2B2B28` → 未传色时读主题 ink（`defaultInk()`：浅色用 THEMES[theme].ink，深色用 #EDE7D9）——SVG data URI 无法用 CSS 变量的约束下在组件内解析主题字面色 | node --check 过 |
| MP-17 | 新建 `utils/toast.js`（统一 duration 2500ms 适老化 + ok/err/info 快捷方法）；**17 个文件 118 处** `wx.showToast` 全量替换并自动插入 require；修正首轮层级 bug（../ 数量按目录深度重算） | `grep wx.showToast pages/ components/` 为 0；**全量 require 路径存在性验证 broken=0**；全部 JS node --check 过 |

### 关键教训（已记入 handoff）
- Git Bash 下 `python -c "..."` 内嵌正则的 `\s`/`\b` 会被吞掉导致静默不匹配——**正则脚本必须写临时 .py 文件执行**（本轮白色上下文替换因此漏了一轮，第二轮用文件方式补齐）。
- 批量替换后的 require 相对路径需用脚本验证存在性（os.path.normpath 解析），不能信拼接逻辑。

**Wave 3 剩余（下一轮）**：MP-10/11/12/19（类名收敛/自绘空态/三态补齐/杂项）、WEB-04~12/15~26。

## 🔄 第八轮执行（MP-10/11/12/19 + WEB-04/05，共 6 项）
| ID | 改动 | 验证 |
|---|---|---|
| MP-10 | **卡片/页头/top-bar 收敛**：① 15 个遗留卡片类（addr/overview/ho/route/info/desc/carrier/proof/stops/weather/detail/n/fb/group/quote/review-card）wxml 挂全局 `.card` 兜底视觉一致，wxss 删除与 .card 重复的 bg/radius/shadow/margin-bottom/padding（布局与特化保留：n-card 左竖条 padding、overview 渐变头卡、quote 高亮底、reach 提示条全保留）；② product-card 不挂 card（横滑紧凑卡，加 margin-bottom 会破坏布局，视觉已走令牌）；③ `page-header`→`page-head`（2 wxml + 2 wxss + app.wxss 合并双写法）；④ `.top-bar` 三份复制收敛入 app.wxss（padding-bottom 统一 48rpx） | `grep page-header pages/**/*.wxss` **=0**；括号配对 PASS；骨架类（sk-n-card/sk-order）零误伤（负向断言） |
| MP-11 | 自绘空态/错误横幅统一 `empty-state` 组件：① 组件加 slot（es-text 与 es-sub 之间，容纳 chip 富内容）+ `iconColor` property（错误态红图标覆盖默认主题色）；② index 页 off/error 态走组件（error 带"重新加载"按钮）；③ empty 态（含线路/站点 chip 列表）走组件 + slot chips；④ workbench `load-error-banner` 手写横幅改组件 + json 注册组件 | 删除 .bus-empty-title/.bus-retry/.load-error-* 死样式；同屏空态长相统一 |
| MP-12 | 补齐缺失状态：① index 公告失败不再静默落"暂无公告"（误导），新增 `noticesError` 三态分支（error 带 retryNotices 重试按钮）；② profile 新增 `profileLoaded` 字段级骨架（3 行骨架条），失败用本地缓存 userInfo 兜底预填不让骨架常驻；③ **send 页复核达标不改**：网络依赖仅 loadStations（station-picker 已有 loading/error/retry 三态）+ reachability（已有 reachLoading 卡），表单为同步草稿，整页骨架无必要 | 13/13 测试全绿 |
| MP-19 | 杂项 6 项：① mine.js 删 `role: 'consumer'` 死字段，user-role-tag 图标 home→user（MP-13 遗漏）；② config.js develop HTTP 加安全警示注释（HTTPS 化需服务器侧支持=人工项，保持现状）；③ api.js 未登录不再发空 `Authorization: ''` 头（条件构造 header）；④ 删死接口 `getDefaultAddress`/`getRealtimeBuses`（定义+导出，全项目零调用）；⑤ README 测试数 10→13；⑥ app.js UpdateManager 补 `onUpdateFailed` 失败提示（onUpdateReady 已有） | 全部改动 node --check 过 |
| WEB-04 | `styles/index.scss` 表头 `#f4f8f4/#2b2b28` 写死浅绿 → `var(--el-fill-color-light)` / `var(--el-text-color-primary)`，深色模式随主题翻转 | grep f4f8f4=0 |
| WEB-05 | `text-gray-400` → `text-gray-500`（6 文件 21 处，dashboard/dispatch/monitoring×2/order/productOrder）；#909399 已在 WEB-03 清零（复核确认） | grep text-gray-400 transport=0 |

### 本轮发现并修复的测试盲区
- `tests/icon-names.test.js` 解析 ICONS 表的正则不匹配**带引号键**（`'chevron-right':` 含连字符必须带引号定义），导致上轮新增图标被误报"不存在"→ 正则改为 `(?:['"]?)([a-zA-Z][\w-]*)(?:['"]?):` 兼容两种写法。
- 项目 emoji 禁令覆盖**注释**（config.js 的 ⚠️ 被静态扫描拦截）→ 改纯文本标注。

**Wave 3 小程序全部完成（MP-01~19）**。JS 全部 node --check 过；**测试套件 13/13 全绿**。
**Wave 3 剩余（下一轮）**：WEB-06~12/15~26（Dialog 遮罩/绕过封装/术语统一/状态映射/静默 catch/空态/坐标转换去重等）。

## 🔄 第九轮执行（WEB-06/07/08/09，共 4 项）
| ID | 改动 | 验证 |
|---|---|---|
| WEB-06 | Dialog 组件新增 `closeOnClickModal` prop **默认 false**——原模板硬编码 `:close-on-click-modal="true"` 且显式绑定排在 v-bind 之后，使用方传参被覆盖无法关闭误关。现在表单弹窗点遮罩不丢数据；个别需旧行为可显式传 true | vue-tsc 零错误 |
| WEB-07 | 3 处绕过改回 Dialog 封装（handover 争议 / notification 发送通知 / station 地图选点，后者 append-to-body 保留）；弹窗宽度收敛 3 档：1180→1000、640/650/760/900→720、500×2/550×8/560→480 | `grep "<el-dialog" src/views/transport/` **=0**；弹窗 width 只剩 480/720/1000（100/120/150/80/90px 为 el-form label-width，非弹窗） |
| WEB-08 | **上下架枚举统一 1=上架**（以公告为准，改商品侧，前后端联动）：后端 ProductMapper 两处 `eq(status,0)→1`、ProductOrderServiceImpl 下架校验 `!=0→!=1`、3 个 VO Schema 注释、transport-schema.sql DEFAULT 0→1；前端 product/index.vue 筛选+tag、ProductForm.vue 单选+默认值翻转；**新增 V026__product_status_flip.sql** 存量数据翻转（`UPDATE SET status=1-status`，标注不可重复执行）。术语统一 21 处：场站→站点（17）、网点→站点（1）、转接站→换乘站（4）、智能调度→智能派单（含 topology 页） | grep 场站/网点/转接站/智能调度 = 0；模块编译过；mvn test 见下轮 |
| WEB-09 | **constants.ts 修正+扩充**：① VEHICLE_STATUS 修正为权威语义 `0可用/1停用`（旧表误写 0空闲/1在途/2停用，混入 monitorStatus 语义——VehicleDO 权威注释为「0 空闲可用，1 停用维修」）；② 新增 VEHICLE_REALTIME_STATUS（0空闲/1在途/2故障/3离线，VehicleDO.realtimeStatus）；③ 新增 PRODUCT_ORDER_STATUS（0待发货/1已发货/2已完成/3已取消，ProductOrderStatusEnum）。5 处绕过全部改消费 constants：vehicle 页两 tag、DispatchVisualDialog actionLabel（站点发车/乘客上车→出发/接客，与后端 3 处权威一致）、dispatch 两个筛选下拉改从 LABELS map 生成、dashboard 本地 ORDER_TYPE_LABELS 删除、productOrder statusMap 删除 | vue-tsc 零错误；同一状态全站同一文案 |

### WEB-08 关键决策：数据语义翻转的迁移安全
- 枚举翻转属**破坏性数据语义变更**：全新环境用最新 transport-schema.sql（DEFAULT 1）；已有环境必须执行 V026 增量（含不可重复执行警示）。
- 排查确认影响面：后端测试无商品 status 断言、小程序零消费（goods 列表走后端过滤接口，后端已改）。

### WEB-09 关键发现：constants.ts 自身有错
「三者冲突」实为两个不同字段：VehicleDO.status（0可用/1停用）vs VehicleDO.realtimeStatus（0空闲/1在途/2故障/3离线）。vehicle/index.vue 本地映射本来就是对的，**错在 constants.ts 把两个字段语义合并了**。修 constants 而非页面——单一来源也要保证来源正确（先核对后端权威注释再改）。

## 🔄 第十轮执行（WEB-10/11/12，共 3 项）
| ID | 改动 | 验证 |
|---|---|---|
| WEB-10 | 全量扫描 57 处 catch 分类（HAS_MSG 3 / COMMENT_ONLY 28 / OTHER 26）。**合理静默保留**：confirm 用户取消（`return`/`cancelled`，dispatch×5/order×1 等）、请求层已提示（×2）、页面级错误横幅（bigscreen/monitoring/replay 的 mapError 机制）。**补可见提示 6 处**：order:276 站点兜底失败（清单点名）、dashboard 结算/总览 2 处 console.error、replay 车辆列表/轨迹 2 处 console.error；**monitoring 轮询特殊处理**：refreshData 加 silent 参数——首次加载失败弹一次，15s 轮询失败仅记日志保留旧数据（弱网不轰炸，与小程序 MP-17 同理） | vue-tsc 零错误 |
| WEB-11 | **19 张表格补 `#empty` 插槽**（15 文件）：7 个资源管理页（商品/站点/线路/班次/司机/车辆/公告）带「新增 XX」引导按钮（openForm('create')）；订单池/方案/交接/通知/商城订单/绑定/反馈/明细等 12 张查询结果表给语境化描述（如「订单池暂无订单——审核通过的订单归集后会出现在这里」）；expiry 两 tab 区分驾照/保险 | el-empty 文件 4→17，总 31 处 |
| WEB-12 | 坐标转换去重：StationForm（gcj02ToBd09+bd09ToGcj02+X_PI）、DispatchVisualDialog（gcj02ToBd09+X_PI）本地实现删除，统一 import `@/components/Map`（index.ts 补导出两个函数）；**MapDialog.vue 259 行死代码删除**（全项目零引用，StationForm 的选点弹窗已是业务定制版） | `grep "const gcj02ToBd09" src/` **仅剩 utils.ts 1 处**；auto-components.d.ts 残留声明已清 |

### WEB-10 方法论：catch 分类决策树
57 处 catch 分四类处理：①已有 message/横幅→不动；②confirm 取消/cancelled→合理静默保留；③console.error-only→**用户主动动作的补 message.error**，④轮询类→silent 参数首次弹/轮询静默。避免一刀切全弹导致弱网轰炸。

## 🔄 第十一轮执行（WEB-15~26，共 12 项）
| ID | 改动 | 验证 |
|---|---|---|
| WEB-15 | **echarts 主题注册**：`plugins/echarts/index.ts` 新增亮色主题 `cargo` + 导出 `CARGO_CHART_COLORS`（8 色，山泉蓝打头）；dashboard 3 处内联 `color: ['var(--status-success)',…]` 改用常量——**关键认知：echarts canvas 不继承 CSS 变量，原内联 `var(...)` 实际全部失效（图表一直是 echarts 默认色）** | vue-tsc 零错误 |
| WEB-16 | 表单校验补齐：notification 补 `sendRules` 字段级校验（收件范围/标题/内容），handover 补 disputeForm 校验（争议原因必填 + 处理说明长度），route/RouteForm 补站点序列非空校验 | 校验失败走 el-form 行内提示，不再靠 message.warning 手拦 |
| WEB-17 | 二次确认：handover 争议提交、notification 发送通知（影响全量司机）均加 `ElMessageBox.confirm` 二次确认 | — |
| WEB-18 | 键盘可达性：车辆列表项/图例/面板 tab/route-card 补 `role="button"` + `tabindex="0"` + `@keydown.enter/space.prevent`；全局补 `[role="button"]:focus-visible` 焦点环（styles/index.scss） | — |
| WEB-19 | 长文本 tooltip：order 页物品信息/goodsName 超 12 字显示 tooltip（`:disabled` 控制只在溢出时生效） | — |
| WEB-20 | dispatch-center 订单池真分页（`poolQuery{pageNo,pageSize:20}` + `poolTotal` + Pagination 组件），此前 pageNo=1/pageSize=100 写死，超出 100 条不可见 | — |
| WEB-21 | dashboard 根节点补 `v-loading="loading"`（此前无加载态，接口慢时白屏无反馈） | — |
| WEB-22 | driver/vehicle/route/shift 四页 `resetQuery` 全字段清空（此前只重置部分字段，残留筛选条件难察觉） | — |
| WEB-23 | 禁用态替代隐藏：order 审核按钮 `v-if` 改 `:disabled` + `el-tooltip`（"仅货运待审核订单可审核"）——避免权限/状态不符时按钮凭空消失 | — |
| WEB-24 | ContentWrap 嵌套消除：order（3 层→2 层）、dispatch 内层两块改 `section.dispatch-section` + `h3` 标题（卡片套卡片视觉噪声） | — |
| WEB-25 | 响应式降级：monitoring 侧栏 `w-340px`→`w-full lg:w-340px`；dispatch-center 三列栅格加 xs/sm/md/lg 断点（窄屏改单列堆叠）；DispatchVisualDialog min-width 改 `min(420px, 100vw - 48px)` | — |
| WEB-26 | 杂项 8 项：删幽灵接口 `getTransportModuleStatus`（后端无此映射）、删除后页码回退、topology load 补 try/catch + message.error、SMS 日志页失败原因列、dispatch 筛选下拉从 LABELS 生成、系统短信日志 apiSendCode/apiSendMsg 展示等 | eslint 全绿 |

## 🔄 第十二轮执行（收尾：ENG-04 落地 + SEP-05 聚合 + 全量勾选）
| ID | 改动 | 验证 |
|---|---|---|
| ENG-04 | **前端接入 vitest**：`package.json` 加 `"test": "vitest run"` + devDep `vitest@^3.2.4`；新建 `vitest.config.ts`（仅 `src/**/__tests__/**/*.test.ts`，node 环境，不引 Vue 挂载）；**3 个测试文件 40 用例**：① `views/transport/__tests__/constants.test.ts` 17 例——枚举契约（订单状态 0-13 全集锁定、LABELS↔TAGS 键集合一致、车辆静态状态 vs 实时状态语义分离的 WEB-09 回归、labelOf/tagOf 兜底）；② `utils/__tests__/tree.test.ts` 9 例——listToTree/treeToList/findNode/handleTree/filter（**发现 listToTree 会原地突变入参且不可重入**，测试改用工厂函数，已写入注释）；③ `components/Map/src/__tests__/utils.test.ts` 14 例——坐标往返精度（1e-6）、网格聚合分桶（同格合并/跨边界不合并/增量均值/空输入/对象引用保留）、阈值、气泡三阶配色、暗色样式必备要素 | `pnpm test` **40/40 全绿**（3 文件 1.7s）；`ci.yml` frontend job 加 `pnpm test` 门禁（lockfile 已锁 vitest 3.2.7，`--frozen-lockfile` 可用） |
| WEB-14 + SEP-05 | **marker 网格聚合 + 暗色底图**（两项同一实现，此前均未做）：① `components/Map/src/utils.ts` 新增 `clusterByGrid`（把点用 `map.pointToPixel` 投到屏幕像素后按 64px 网格分桶，桶内多点合成数量气泡，位置取增量均值）/`shouldCluster`（>40 点启用）/`clusterBubbleStyle`（数量分三阶加深，白字白描边）/`BIGSCREEN_MAP_STYLE`（百度 GL `setMapStyleV2` 暗色 styleJson，深蓝墨 `#0B1220` 底）。**未用官方 MarkerClusterer**——该库面向 BMap 2.0、对 GL 支持不完整且需额外 CDN 脚本（答辩现场网络不可控），自研零依赖且可单测。② `monitoring`：低密度逐车增量渲染（标签全可读）与聚合渲染互斥切换，`zoomend` 重算网格（放大自然散开为单车），页面左上角显示"已按网格聚合显示"提示避免误判"车少了"。③ `dispatch-center`：站点 Circle+Label 与车辆 Marker+Label 统一走 `renderBaseOverlays`（超阈值聚合），补 `onUnmounted` 清理。④ `bigscreen`：优先 `setMapStyleV2` 官方暗色样式，**SDK 不支持时自动保留半透明遮罩兜底**（`shadeEnabled`），车辆 T3 刷新也走聚合 | vue-tsc 零错误；eslint 全绿；聚合算法 14 例单测覆盖；500+ 点位验收指标由阈值+网格保证（原每点 2 个覆盖物 → 每桶 1 个） |

### 本轮关键认知
- **echarts 与 CSS 变量**：canvas 渲染不继承 CSS 变量，图表配色必须字面值/主题注册——这条同样适用于任何 canvas/WebGL 场景（大屏地图 Label 同理）。
- **聚合不该无条件开**：低密度场景聚合反而降低可读性（用户看不到车牌），因此设计为阈值触发（>40）+ 缩放自适应，而非"一律聚合"。
- **百度地图自绘聚合 vs 官方库**：GL 版本没有可用的官方 MarkerClusterer CDN 包，`map.pointToPixel` 自研网格是更稳的路径，且纯函数可测。
- **测试数据共享陷阱**：模块级常量数组被被测函数原地突变，会让同文件后续用例随机失败（tree.test 首轮 2 例失败即此因）——测试数据一律用工厂函数生成。

**收尾状态：110 项中 105 项已勾选**（P0 14/14 全部完成）。未完成 5 项：ENG-01/ENG-03（需真实 DB / Testcontainers，已给出落地路径）、BE-03（部分完成，保留 `.env*` 跟踪以不打断部署链路）、BE-10（待业务确认权限维度）、SMS-03（人工，需企业资质）。

