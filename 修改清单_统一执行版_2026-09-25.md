# cargo-post 统一修改清单 · AI 执行版

> **版本** v1.0 ｜ **生成日期** 2026-09-25 ｜ **融合来源**：`审计报告_全景_2026-09-25.md`、`UI审计与统一设计系统方案_2026-09-25.md`（v2 山泉蓝）、`诊断_短信验证码收不到_2026-09-25.md`  
> **本文档是唯一事实来源**。若与其它报告冲突，以本文档为准（已按 v2 山泉蓝配色决策统一口径）。

---

# 第 0 部分 · 给执行 AI 的作业规则（必读，逐条生效）

## 0.1 任务目标

在不破坏现有功能的前提下，完成本清单列出的修复与改造。**答辩临近，Wave 0 与 Wave 1 优先。**

## 0.2 硬性边界（违反即视为失败）

| #  | 规则                                                        |
| -- | --------------------------------------------------------- |
| 1  | **只改本清单列出的点**。不做任何清单外的"顺手重构""顺便优化""代码美化"。                 |
| 2  | 改任一文件前**必须先读该文件**，改动后再次核对。禁止凭记忆改代码。                       |
| 3  | **最小 diff**。禁止无必要地全文重写、重排 import、调整无关格式。                  |
| 4  | 禁止删除或重命名任何现有公共方法/类/接口/枚举值（除非本清单明确要求）。                     |
| 5  | 禁止修改数据库已有列的类型与名称。只允许**新增**列/索引（本清单列出时）。                   |
| 6  | 禁止跳过测试、禁止 `@Disabled` 掉失败的测试、禁止把断言改成 `log`。测试失败要报告，不要掩盖。  |
| 7  | 遇到本清单未覆盖的歧义、或改动会导致行为不确定时 —— **停下来提问**，不要猜。                |
| 8  | 每个 Wave 完成后**必须运行第 4 部分的验证命令**并把输出贴出来。                    |
| 9  | 以下内容一律**不要动**：见第 5 部分「不改清单」。                              |
| 10 | 所有 UI 颜色/圆角/间距/字号**必须引用令牌变量**，禁止写 hex 字面量（第 1 部分是令牌唯一来源）。 |

## 0.3 任务条目字段说明

每条任务包含：

```
ID          唯一编号（用第 2 部分的表格做进度勾选）
严重度      P0 阻断 / P1 严重 / P2 中等 / P3 轻微
端          小程序 / Web管理端 / 后端 / 数据库 / 部署 / 测试
文件:行号   定位（行号为 2026-09-25 快照，若已漂移请按代码内容定位，不要盲改）
现状        代码原文或现象
改为        明确的目标状态
验收        可执行的判定标准
```

## 0.4 执行顺序

```
Wave 0  配置对齐（答辩前置，约 1 天，零风险）
Wave 1  安全与数据一致性（后端 P0/P1）
Wave 2  容量与韧性（后端 + 部署）
Wave 3  UI 令牌下沉与缺陷修复（小程序 + Web）
Wave 4  小程序闪屏页与启动链路
Wave 5  短信验证码（测试环境先恢复可用）
Wave 6  智慧大屏（数据层 → 视觉层）
Wave 7  工程与测试补齐
```

> Wave 内任务可并行；Wave 之间有依赖的已在条目中标注 `依赖:`。

---

# 第 1 部分 · 全局设计令牌（UI 类改动的唯一事实来源）

## 1.1 配色决策（v2）

**主色 = 山泉蓝 `#1F5E9E`**（国赛答辩主题，三端统一）。站牌绿 `#2E7D32` **已降级为成功色**。

| 语义                      | 值         | 用途                                |
| ----------------------- | --------- | --------------------------------- |
| `--brand-primary`       | `#1F5E9E` | 主色：按钮 / 选中 / 链接 / 导航栏             |
| `--brand-primary-dark`  | `#123F6E` | 深主色：页头 / 导航栏                      |
| `--brand-primary-light` | `#E6EFF5` | 浅主色：选中底 / 标签底                     |
| `--brand-accent`        | `#2E7BBF` | 强调 / 进度亮点                         |
| `--brand-clay`          | `#C75B2A` | 陶土橙：**价格 / 司机 / 行动**（点缀色，非状态色）    |
| `--brand-clay-light`    | `#F8ECE3` |                                   |
| `--brand-clay-deep`     | `#A64A1F` |                                   |
| `--brand-gold`          | `#D9A441` | 稻谷金：**农产品 / 公告**（点缀色，非状态色）        |
| `--brand-gold-light`    | `#FBF3DF` |                                   |
| `--brand-gold-deep`     | `#8A6A1F` |                                   |
| `--status-success`      | `#2E7D32` | 成功 / 已完成 / 在线 / 正向                |
| `--status-warning`      | `#B27A12` | 警告 / 临近到期                         |
| `--status-danger`       | `#C0392B` | 危险 / 删除 / 异常                      |
| `--status-pending`      | `#C75B2A` | 待处理                               |
| `--status-done`         | `#8A8778` | 已结束（中性灰）                          |
| `--text-primary`        | `#2B2B28` | 正文                                |
| `--text-secondary`      | `#6B675C` | 辅助文字（5.55:1 ✅）                    |
| `--text-tertiary`       | `#8A8778` | **仅限 ≥32rpx 文字或纯装饰**（3.63:1，小字禁用） |
| `--text-disabled`       | `#A8A294` | 禁用                                |
| `--surface-page`        | `#F5F8FC` | 页面底                               |
| `--surface-card`        | `#FFFFFF` | 卡片                                |
| `--surface-card-warm`   | `#F0F5FA` | 票根 / 副券                           |
| `--surface-field`       | `#F7FAFD` | 输入框底                              |
| `--surface-line`        | `#DCE6EE` | 描边                                |
| `--surface-line-deep`   | `#C7D5E0` | 分隔 / 虚线                           |
| `--surface-track`       | `#E4EBF2` | 轨道 / 浅填充                          |

> ⚠️ **两个蓝不要混用**：Element 默认蓝 `#409eff`（亮）≠ 山泉蓝 `#1F5E9E`（哑光深蓝）。  
> ⚠️ **不要说 `--status-info`**：蓝已是品牌主色，info 语义已取消，提示类标签用 `--status-pending` 或中性灰。

## 1.2 间距 / 圆角 / 字号阶梯

```
间距   --space-1  4px/8rpx    --space-2  8px/16rpx   --space-3  12px/24rpx
       --space-4  16px/32rpx  --space-5  24px/48rpx  --space-6  32px/64rpx
       → 禁止 13rpx / 18rpx / 27rpx 等非阶梯值

圆角   --radius-s 8rpx/4px    小控件（标签、徽标）
       --radius-m 16rpx/8px   输入框、按钮   ← 归并 20rpx(41处)、14rpx(23处)
       --radius-l 24rpx/12px  卡片、弹窗     ← 归并 18rpx、22rpx
       --radius-xl 32rpx/16px 大面板（大屏）
       --radius-pill 999rpx   胶囊

字号   --font-caption 24rpx/12px   票号/单位（仅非关键信息）
       --font-body-s  28rpx/14px   辅助说明
       --font-body    32rpx/14px   正文基准   ← 当前 233 处低于此值
       --font-title-s 36rpx/16px   卡片标题
       --font-title   40rpx/20px   区块标题
       --font-display 56rpx/24px   金额/核心数字
```

**适老化基线**：小程序正文不得低于 `28rpx`；可点击区 ≥ `88rpx`（≈44px）。

## 1.3 图标规范

| 规则  | 内容                                                                                         |
| --- | ------------------------------------------------------------------------------------------ |
| 尺寸档 | 小程序 `24/32/48/64` rpx；Web `16/20/24/40` px                                                 |
| 禁止  | 文本字形充当图标（`▾` `›` `→`）；emoji                                                                |
| 需新增 | `caret-down`（下拉）、`chevron-right`（前进/更多）                                                    |
| 需拆分 | `box`→`box`/`cart`/`order`；`notice`→`bell`/`megaphone`/`bulb`；`sun`→`sun`/`moon`/`palette` |
| 颜色  | 必须绑定主题变量（`iconColor`/`iconDeep`/`iconAccent`），禁止字面量                                        |

## 1.4 组件状态规范

| 组件  | 必填                                                                      | 规范                                                            |
| --- | ----------------------------------------------------------------------- | ------------------------------------------------------------- |
| 空态  | 插图 + 主文案 + 副文案 + **行动按钮**                                               | 小程序用 `empty-state` 且**必须传 `btnText`**；Web 用 `el-empty` + 插槽按钮 |
| 加载态 | 骨架屏（列表）/ 按钮 loading（提交）                                                 | 骨架数量 = 预期条目数 3~5                                              |
| 错误态 | 与空态同构 + **必带可见重试按钮**                                                    | 禁止"点父容器重试"这种不可见交互                                             |
| 弹窗  | 标题 + 内容 + 「取消/确定」                                                       | 表单类必须 `close-on-click-modal=false`                            |
| 卡片  | `.card`：`--surface-card` + `--radius-l` + `--shadow-card` + `--space-4` | 收敛现有 18 个卡片类名                                                 |
| 页头  | `.page-head`：`--brand-primary-dark` 渐变 + 落款横档 + 左右 `--space-4`          | 删除 `.page-header` 写法                                          |

## 1.5 令牌落地位置

| 端   | 落地文件                                                          | 说明                                 |
| --- | ------------------------------------------------------------- | ---------------------------------- |
| 小程序 | `cargo-post-miniprogram/app.wxss`（`page { --xxx }`）           | 已有令牌结构，补齐上表缺项即可                    |
| Web | `yudao-ui/yudao-ui-admin-vue3/src/styles/tokens.scss`（**新建**） | 在 `styles/index.scss` 顶部 `@use` 引入 |

---

# 第 2 部分 · 任务清单

## Wave 0 · 配置对齐（答辩前置 · 零风险 · 先做）

> 本 Wave 全部是改常量/色值，不改逻辑。**做完即可让三端视觉一致。**

### CFG-01 【P0】Web 管理端主色改为山泉蓝

- **端**：Web 管理端
- **文件**：`yudao-ui/yudao-ui-admin-vue3/src/store/modules/app.ts:77`
- **现状**：`elColorPrimary: '#2e7d32',`
- **改为**：`elColorPrimary: '#1F5E9E',`
- **验收**：登录管理端，主按钮 / 选中菜单 / 链接为哑光深蓝；`grep -rn "2e7d32" src/` 仅剩 `styles/index.scss` 的成功色一处
- **备注**：这是三端统一的关键一行，收益最大

### CFG-02 【P0】Web 设置面板色板首项替换（防止退化成 Element 默认蓝）

- **端**：Web 管理端
- **文件**：`src/layout/components/Setting/src/Setting.vue:218-229`
- **现状**：`:schema="['#409eff','#009688','#536dfe','#ff5c93','#ee4f12','#0096c7','#9c27b0','#ff9800']"`
- **改为**：`:schema="['#1F5E9E','#123F6E','#2E7BBF','#2E7D32','#C75B2A','#D9A441']"`
- **验收**：设置抽屉里点为第一个色块，站点保持山泉蓝，不再变成亮蓝 `#409eff`
- **依赖**：CFG-01

### CFG-03 【P0】Web 语义色解耦确认（主色转蓝后 success 保留绿）

- **端**：Web 管理端
- **文件**：`src/styles/index.scss:39-45`
- **现状**：`--el-color-success: #2e7d32;`（与旧主色同值，导致"进行中"与"已完成"标签无法区分）
- **改为**：**保持 `#2e7d32` 不动**。仅确认它不再与 primary 冲突即可。
- **验收**：`el-tag type="success"`（绿）与 `type="primary"`（蓝）视觉可区分
- **备注**：CFG-01 完成后此问题自动消解，**不需要额外改动**

### CFG-04 【P1】Web 页面底色统一为山泉蓝主题底

- **端**：Web 管理端
- **文件**：`src/styles/var.css:49-53`
- **现状**：`--app-content-bg-color:#f5f7f9;`
- **改为**：`--app-content-bg-color:#F5F8FC;`
- **验收**：内容区底色与小程序的 `--surface-page` 一致（冷白族）

### CFG-05 【P1】小程序三处默认主题统一为山泉蓝

- **端**：小程序
- **文件**：
  - `app.json:27` → `"navigationBarBackgroundColor": "#123F6E"`（现 `#1C4B2E`）
  - `app.wxss:14` → `--color-primary: #1F5E9E`（现 `#2E7D32`）
  - `app.js:11` → `themeColor: wx.getStorageSync('themeColor') || 'blue'`（现 `'green'`）
- **理由**：`utils/appearance.js:19` 的 `DEFAULT_THEME = 'blue'` 保持不变，但这三处目前是绿色系，导致冷启动首帧深绿导航栏 → 主题应用后变蓝的"闪色"
- **验收**：冷启动导航栏、页面主色、主题三者一致，无可见颜色跳变
- **依赖**：无

### CFG-06 【P1】小程序 tab 栏深色模式失效

- **端**：小程序
- **文件**：`custom-tab-bar/index.js:41`
- **现状**：`themeStyle: appearance.themeStyle(s.themeColor)` —— 只传 1 个参数
- **改为**：`themeStyle: appearance.themeStyle(s.themeColor, s.darkMode)`（签名见 `utils/appearance.js:163`）
- **验收**：开启深色模式后底部 tab 栏为深色，不再浅色发光
- **备注**：同时补 `app.js` 的 `globalData` 增加 `darkMode` 字段（`pages/settings/settings.js:42` 已在写这个字段但未声明）

### CFG-07 【P2】小程序弹窗按钮色跟随主题

- **端**：小程序
- **文件**：9 处 `confirmColor: '#C75B2A'` 硬编码  
  `pages/index/index.js:343`、`pages/goods/detail/detail.js:201,220`、`pages/mine/mine.js:90`、`pages/mine/address/address.js:232`、`pages/driver/workbench/workbench.js:963`、`pages/driver/handover/handover.js:180`、`pages/send/send.js:756`、`pages/orders/orders.js:221`


- **改为**：从 `appearance.THEMES[当前主题]` 取 `clay`（或新建 `appearance.getClay()` 辅助方法）
- **验收**：切换主题后 `showModal` 的确定按钮颜色跟随变化

---

## Wave 1 · 安全与数据一致性（后端 P0/P1）

> **不做完这个 Wave 不要上生产。** 每条都附带单测要求。

### BE-01 【P0】算法调用超时链收敛（消除 233 秒阻塞）

- **端**：后端
- **文件**：`yudao-module-transport/.../integration/algorithm/AlgorithmProperties.java:24-39`、`AlgorithmClient.java:61-94,227-251`
- **现状**：`readTimeout=15s` × `maxRetries=3`（4 次尝试）+ 3×1s 退避 = 63s；再叠 408 轮询 `pollInterval=2s × maxPollAttempts=10` = 最坏约 233s 单请求阻塞，独占 Tomcat 线程
- **改为**：
  1. `readTimeout: 15s → 8s`
  2. `maxRetries: 3 → 1`
  3. `retryBackoff: 1s → 500ms`
  4. 轮询改为**总预算制**：整体不超过 15s（不再固定 10 次）
  5. 冷却保护从"仅 route/allocate"扩展到 `plan()/distance()/route()/allocate()` 全部四个方法
- **验收**：
  - `docker stop cargo-post-algorithm` 后请求 `POST /admin-api/transport/dispatch/plan/smart`，接口在 **≤20s** 内返回明确失败
  - 期间 `jstack` 观察 Tomcat 线程数不持续增长
- **备注**：改动最小、收益最大，**优先做这条**

### BE-02 【P0】数据源连接池扩容 + 快速失败

- **端**：后端
- **文件**：`yudao-server/src/main/resources/application-local.yaml:34-37`、`application-dev.yaml` 同位置
- **现状**：`min-idle: 1`、`max-active: 20`、`max-wait: 60000`
- **改为**：
  - `max-active: 20 → 50`
  - `min-idle: 1 → 10`
  - `max-wait: 60000 → 5000`
  - 显式补 Tomcat 约束：`server.tomcat.threads.max: 200`、`accept-count: 100`、`max-connections: 2000`
- **验收**：并发 20 个派单请求不再出现 `GetConnectionTimeoutException`；等待超时后 5s 内快速失败而非挂 60s
- **依赖**：BE-01（两条叠加才有效）

### BE-03 【P0】管理端 `.env` 从 Git 移除并轮换凭据

- **端**：Web 管理端 / 仓库
- **文件**：`yudao-ui/yudao-ui-admin-vue3/.env`、`.env.dev`、`.env.local`、`.env.prod`、`.env.stage`、`.env.test`
- **现状**：6 个 `.env*` 文件被 git 跟踪（`git ls-files` 已确认），`.env:23-31` 含 AES 对称密钥、`admin/admin123`、百度地图 AK
- **改为**：
  1. `git rm --cached yudao-ui/yudao-ui-admin-vue3/.env*`
  2. `.gitignore` 增加 `yudao-ui/**/.env*` 与 `!yudao-ui/**/.env.example`
  3. 新增 `yudao-ui/yudao-ui-admin-vue3/.env.example` 模板（值留空）
  4. 删除 `.env` 的 `VITE_APP_DEFAULT_LOGIN_USERNAME/PASSWORD`，并删除 `src/views/Login/components/LoginForm.vue:121-122` 的预填逻辑
  5. `VITE_APP_API_ENCRYPT_ENABLE = false`（前端不做 API 加解密）
  6. 把现有 CI 的 `.env` 拒绝跟踪门禁（`.github/workflows/ci.yml:191-197`）从 `yudao-ui/` 白名单改为**全仓覆盖**
- **验收**：`git ls-files | grep -E '\.env$' ` 为空；管理端登录页不再预填账号密码
- **⚠️ 人工必做（AI 无法完成）**：轮换 admin 密码、百度地图 AK、AES 密钥 —— 它们已进入 Git 历史，`git rm --cached` 无法消除历史泄露

### BE-04 【P1】订单更新接口禁止直写 status（绕过状态机）

- **端**：后端
- **文件**：`controller/admin/transport/order/vo/TransportOrderBaseVO.java:23`、`service/transport/order/TransportOrderServiceImpl.java:92-95`
- **现状**：`TransportOrderBaseVO` 含 `status` 字段，`update()` 经 MapStruct `convert()` 自动映射后 `updateById()` 直接落库，无状态机校验
- **改为**：从 `TransportOrderUpdateReqVO` 移除 `status` 字段（或在 `update()` 中强制忽略传入的 status）
- **验收**：新增单测——传入 `status` 的更新请求不改变库中状态
- **风险**：需确认前端没有依赖这个字段做状态流转（`views/transport/order/OrderForm.vue` 需一并核对）

### BE-05 【P1】手工派单补车辆在途占用校验

- **端**：后端
- **文件**：`service/dispatch/DispatchServiceImpl.java:401-409`
- **现状**：`createManualPlan` 只调 `validateVehicleExists()`，**未调用 `busyVehicleIds()`**（该方法在 `:1168-1196`，是智能派单的占用判据）
- **改为**：在 `createManualPlan` 中复用 `busyVehicleIds()`，命中则抛业务异常（新增错误码如 `VEHICLE_BUSY`）
- **验收**：单测——同一车辆二次派单抛 `VEHICLE_BUSY`


### BE-06 【P1】装车容量校验改为原子 SQL（消除超载 TOCTOU）

- **端**：后端
- **文件**：`service/transport/driver/DriverAppServiceImpl.java:894-898`（判断）与 `dal/mysql/shift/ShiftExecutionMapper.java:54-59`（`incrementLoadedCount`）
- **现状**：先读 `loadedCount` 与 `capacity` 比较，再独立执行 `loaded_count = IFNULL(loaded_count,0)+1` —— 读与写分离，并发下可超载
- **改为**：把容量条件下推到同一条 UPDATE：  
  `UPDATE transport_shift_execution SET loaded_count = IFNULL(loaded_count,0)+1 WHERE id = ? AND (capacity 不限制 OR IFNULL(loaded_count,0) < :capacity)`  
  影响行数 0 时抛 `DRIVER_CARGO_FULL`
- **验收**：并发 20 次装车，`loaded_count` 不超过 `cargo_capacity`

### BE-07 【P1】商城订单取消改为 CAS（消除库存虚增）

- **端**：后端
- **文件**：`service/transport/order/ProductOrderServiceImpl.java:131-151`
- **现状**：`validateExists` → 读状态判断 → 循环 `restoreStock()` → `updateById()`（无 CAS）。并发/重复取消会多次补库存
- **改为**：
  1. 先执行 CAS：`UPDATE ... SET status=CANCELLED WHERE id=? AND status=PENDING_DELIVERY`，`affected==0` 直接返回
  2. 成功后再 `restoreStock()`
- **验收**：并发 20 次取消，库存只回补一次，不超过初始值

### BE-08 【P1】下单与商城下单补幂等

- **端**：后端
- **文件**：`service/transport/order/TransportOrderServiceImpl.java:141-151`（寄件）、`ProductOrderServiceImpl.java:74-91`（商城）
- **现状**：`@Idempotent` 全模块使用数为 **0**，无幂等键。双击/弱网重试产生重复订单与重复扣库存
- **改为**：使用框架已有的 `@Idempotent`（`yudao-spring-boot-starter-protection`），或引入客户端 `requestId` + 唯一键去重。覆盖 `/app-api/transport/send/create` 与 `/app-api/transport/product-order/create`
- **验收**：同一 requestId 重复提交返回同一订单号

### BE-09 【P1】司机消息已读接口修正越权

- **端**：后端
- **文件**：`controller/app/transport/driver/AppDriverController.java:247-254`
- **现状**：`messageRead(@RequestParam Long id, @RequestParam Long driverId)` 直接信任传入的 `driverId`。同文件 `:232` 有正确写法
- **改为**：改用 `driverAppService.requireCurrentDriver(driverId)` 推导身份，忽略或强校验传入值
- **验收**：用 A 的 token 读 B 的消息被拒（`DRIVER_IDENTITY_MISMATCH`）

### BE-10 【P1】数据权限（跨站点越权读）

- **端**：后端


- **文件**：全模块 `@DataPermission` 使用数 = 0；`yudao-server/src/main/resources/application.yaml:169` `tenant.enable: false`
- **现状**：任何持有 `transport:order:query` 的账号可读全平台订单（含收件人姓名、手机号、取件码、货物照片）
- **改为**：为订单/方案/站点相关查询加 `@DataPermission`，或显式补 `station_id IN (当前用户可见站点)` 过滤
- **验收**：区域账号只能查到本区域订单
- **备注**：影响面较大，建议先与业务确认"区域→站点"的归属模型再动

### BE-11 【P1】匿名接口加频控

- **端**：后端
- **文件**：`controller/app/transport/send/AppSendController.java:482-511`（`@PermitAll` 的 route-preview / quote / reachability）
- **现状**：匿名可访问、无频控，每次触发一次算法服务调用（进而打高德配额）
- **改为**：加 `@RateLimiter`（IP 维度，建议 60s/20 次）；同时确认 `application.yaml:103` 的 `req-frequency-limit-enable` 开启
- **验收**：压测 100 QPS 时被限流，而非打满线程

### BE-12 【P1】关闭 Druid 控制台匿名访问 + 收敛 Actuator

- **端**：后端
- **文件**：
  - `yudao-module-infra/.../framework/security/config/SecurityConfiguration.java:30`（`registry.requestMatchers("/druid/**").permitAll()`）
  - `yudao-server/src/main/resources/application-local.yaml:17-22`（`allow`/`login-username`/`login-password` 三项均为空）
  - `application-local.yaml:149-154` 与 `application-dev.yaml:101-106`（`exposure.include: '*'`）
- **改为**：
  1. Druid 控制台 `stat-view-servlet.enabled: false`（或设强口令 + IP 白名单）
  2. Actuator `include: '*'` → `include: 'health,info'`
- **验收**：未授权访问 `/druid/index.html`、`/actuator/env` 返回 403/404

### BE-13 【P1】长事务拆解 —— reviewPlan

- **端**：后端
- **文件**：`service/dispatch/DispatchServiceImpl.java:1244-1270`（`@Transactional` 方法内）、`:1359-1412`（循环内 `sendWxaSubscribeMessage` + `sendToDriver`）、`:1273-1285`
- **现状**：事务内做 N×2~3 次微信 HTTP + 循环写库；异常被 `catch(Exception)` 吞掉不清除连接占用
- **改为**：把通知移出事务，改 `TransactionSynchronization.afterCommit` 或事件表 + 定时投递
- **验收**：方案审核的事务持续时间与车辆数无关（用 `SHOW PROCESSLIST` 或 AOP 计时验证）

### BE-14 【P1】嵌套事务拆解 —— planLegs

- **端**：后端
- **文件**：`service/dispatch/MultiLegServiceImpl.java:188-190`（`REQUIRES_NEW`）、`:281-283`、`:470-504`（内层调高德）
- **现状**：外层事务内嵌 `REQUIRES_NEW`（占 2 条连接），内层事务中调用外部 HTTP（最坏 3 段 × 约 9s）
- **改为**：`enrichWithRoadRoute` 移出 `REQUIRES_NEW` 事务，改为事务提交后异步补全
- **验收**：单次派单不再同时持有 2 条连接

### BE-15 【P1】冲突检测去掉全表载入

- **端**：后端
- **文件**：`service/dispatch/LegConflictServiceImpl.java:92-108`
- **现状**：`legMapper.selectList()` 无 where 无 limit，把整张 `transport_leg` 载入内存，再对全量 orderIds 做 `selectBatchIds`
- **改为**：改为按时间窗 + 状态 + 车辆/线路维度的条件查询，禁止无条件 `selectList()`
- **验收**：`transport_leg` 造 10 万行后该接口内存占用与耗时不再线性劣化

### BE-16 【P1】站点全表扫描 N+1

- **端**：后端
- **文件**：`service/transport/station/StationServiceImpl.java:45`（`getSimpleList()` = `mapper.selectList()` 无缓存无 limit）
- **现状**：`AppSendController.java:623-624,234-236,256-257` 等处在**每单**调用 `getSimpleList()`；`track` 详情单次请求调用 3 次
- **改为**：加 Redis 缓存（站点数据几乎不变，TTL 可 10 分钟）或改为按 ID 批量查询
- **验收**：订单列表接口的 SQL 次数与页大小无关

### BE-17 【P1】报表接口优化（含管理端 10s 轮询）

- **端**：后端 + Web 管理端
- **文件**：`controller/admin/dashboard/DashboardController.java:93-113`（全量全表扫描 + 非 sargable 分组）；`monitoring/index.vue:116`（`POLL_INTERVAL = 10000`）
- **改为**：
  1. 后端：改为按日预聚合表，或至少加时间下界
  2. 前端：轮询 10s → **30s**
- **验收**：单次概览接口 SQL 数 < 10、耗时 < 200ms

### BE-18 【P1】关键索引修复（多租户关闭导致的左前缀失效）

- **端**：数据库
- **文件**：`sql/mysql/transport-schema.sql` 各 `idx(tenant_id, ...)`；`sql/incremental/V023__settlement_schema_check.sql:31-34`
- **现状**：`application.yaml:169` `tenant.enable: false`，但所有复合索引以 `tenant_id` 为前导列 → 查询无法命中
- **改为**（新增迁移脚本，只加不删）：
  1. 重建前导列不含 `tenant_id` 的索引，如 `idx_order_pool(status, earliest_pickup_time)`
  2. 补 `dispatch_plan_item(vehicle_id)`、`algorithm_request(snapshot_hash, create_time)`、`shift_execution(driver_id, exec_date)`
  3. `transport_dispatch_plan` 补 `idx_plan_status_create(status, create_time)`（V023 注释已建议但未落地）
- **验收**：`EXPLAIN` 目标查询不再出现 `type=ALL`
- **备注**：先在测试库验证，附 `EXPLAIN` 前后对比

### BE-19 【P1】异常日志脱敏

- **端**：后端
- **文件**：`framework/.../core/handler/GlobalExceptionHandler.java:351,374-378`
- **现状**：错误日志落库含完整请求体（明文密码、手机号、收件地址）
- **改为**：对 `password/mobile/idCard/address` 等字段脱敏，并对 `request_params` 做长度截断
- **验收**：触发一次登录异常后，DB 中 `request_params` 不含明文密码

### BE-20 【P1】异步线程池显式配置（无界队列风险）

- **端**：后端
- **文件**：`framework/.../YudaoAsyncAutoConfiguration.java:15-40`
- **现状**：仅设 `TaskDecorator`，使用默认 `SimpleAsyncTaskExecutor`/无界队列 → 高并发先 OOM 而非拒绝
- **改为**：显式配置 `spring.task.execution.pool.{core-size,max-size,queue-capacity}` 与拒绝策略 `CallerRunsPolicy`
- **验收**：压测时队列有界并出现明确拒绝日志

### BE-21 【P2】订单状态推进补流转校验

- **端**：后端
- **文件**：`service/transport/driver/DriverAppServiceImpl.java:1411-1418`（`updateOrderStatus` 仅 `notIn(终态)`）；`service/dispatch/HandoverServiceImpl.java:424-434` 同型
- **现状**：允许任意跳转（如 `EXCEPTION(13) → DELIVERING(12)`），异常单会被改回正常态
- **改为**：为 `TransportOrderStatusEnum` 补 `canTransit` 流转表（可参考已有的 `TransportLegStatusEnum`），统一入口校验
- **验收**：单测覆盖 14 个状态的合法/非法流转矩阵

### BE-22 【P2】`forceLegStatus` 补状态机守卫

- **端**：后端
- **文件**：`service/dispatch/MultiLegServiceImpl.java:696-710`
- **现状**：`forceLegStatus` 未调用 `canTransit`（`advanceLegStatus:680` 调了），交接流程可强制跳级
- **改为**：加 `canTransit` 校验，或明确限定为"仅管理员 + 记录原因 + 记审计日志"
- **验收**：从 `PLANNED` 直接转 `COMPLETED` 被拒

### BE-23 【P2】批量状态推进校验影响行数

- **端**：后端
- **文件**：`service/dispatch/DispatchServiceImpl.java:3739-3765`
- **现状**：`updateOrdersStatus` 丢弃 `orderMapper.update()` 返回值，部分成功被静默吞掉（调用点 `:1478-1480`、`:1301-1303`、`:1066`）
- **改为**：比对 `affected` 与 `ids.size()`，不等则抛异常并记录差异 ID
- **验收**：单测——部分命中时回滚

### BE-24 【P2】通知幂等唯一键强制化

- **端**：数据库
- **文件**：`sql/incremental/V019__station_access_and_plan_leg.sql:152-159`
- **现状**：检测到历史重复时退化为普通索引（`ADD KEY` 而非 `ADD UNIQUE KEY`）
- **改为**：先清理历史重复数据，再强制建唯一键 `(event_id, user_id, event_type)`
- **验收**：`SHOW INDEX FROM transport_user_notification` 中该索引 `Non_unique = 0`

### BE-25 【P2】发车核验记录补唯一键

- **端**：数据库
- **文件**：`sql/mysql/transport-schema.sql:353`
- **现状**：`transport_departure_check` 无 `(plan_id, vehicle_id)` 唯一键，可重复插入
- **改为**：新增唯一键（先清理重复）
- **验收**：重复发车核验被拒

### BE-26 【P2】分页参数校验补齐

- **端**：后端
- **文件**：`controller/app/transport/send/AppSendController.java:127-130`、`controller/app/transport/product/AppProductController.java:39-42`
- **现状**：`page(PageParam pageParam)` 缺 `@Valid`，`pageSize=-1` 可绕过 `@Min/@Max` 触发不分页全量查询
- **改为**：补 `@Valid`
- **验收**：`GET /app-api/transport/send/page?pageSize=-1` 返回参数校验错误

### BE-27 【P2】错误信息不再透传根因

- **端**：后端
- **文件**：`service/dispatch/DispatchServiceImpl.java:549-557`
- **现状**：把 `Throwable.getRootCause().getMessage()` 拼进业务错误返回前端（泄露表结构/约束名）
- **改为**：对外返回稳定错误码 + 用户可读文案，技术细节只进日志
- **验收**：构造一次算法异常，前端收到的 msg 不含 SQL/类名

### BE-28 【P2】静默异常补日志与计数

- **端**：后端
- **文件**：`service/notification/UserNotificationServiceImpl.java:58-90`（`catch(Exception)` 返回 null）；`service/monitoring/MonitoringServiceImpl.java:416-456`（`catch(Exception ignored)` 且有 `routeProvider` 恒为 `"amap"` 的误导）
- **改为**：至少补 `log.warn` 与失败计数；`routeProvider` 改为按实际来源赋值
- **验收**：通知写入失败时日志可见；轨迹降级时 `routeProvider` 反映真实来源

### BE-29 【P2】算法请求幂等补"进行中"判断

- **端**：后端
- **文件**：`integration/algorithm/AlgorithmAdapter.java:55-71`
- **现状**：`responseJson == null`（同快照请求正在计算中）时不等待、不拦截，直接再插一条并重复调用
- **改为**：进行中时等待或直接返回"处理中"状态
- **验收**：并发同快照请求只产生一次算法调用

### BE-30 【P3】统一事务传播与回滚规则

- **端**：后端
- **文件**：全模块 39 处 `@Transactional`，仅 2 处显式 `rollbackFor`（`DispatchServiceImpl.java:2108`、`RouteServiceImpl.java:81`）
- **改为**：统一补 `rollbackFor = Exception.class`（或明确约定只对 RuntimeException 回滚并写进规范文档）
- **备注**：低风险，但改前需确认无受检异常逃逸场景

### BE-31 【P3】消除自调用导致的事务失效

- **端**：后端
- **文件**：`service/dispatch/MultiLegServiceImpl.java:160-176`（1/2 参重载为 `this` 调用，绕过 `REQUIRES_NEW`）；`service/dispatch/HandoverServiceImpl.java:102-116`
- **现状**：当前未被生产调用（仅测试），属潜在缺陷
- **改为**：改为通过 `AopContext.currentProxy()` 或拆分到独立 Bean
- **验收**：单测验证 `REQUIRES_NEW` 语义生效

---

## Wave 2 · 容量与韧性（部署 + 运行时）

### DEP-01 【P1】发布流程补自动回滚与灰度

- **端**：部署
- **文件**：`.github/workflows/deploy-dev.yml:661-672`（后端 jar 覆盖）、`:678`（前端 rsync 无备份）
- **现状**：健康检查失败只 `exit 1`，**不自动还原**；前端 `rsync -a --delete` 无备份
- **改为**：
  1. 前端 rsync 前备份 `dist.bak`
  2. 健康检查失败自动 `mv *.bak` 回滚并重启
  3. 增加金丝雀步骤（先起 1 实例验证再全量）
- **验收**：注入坏包后自动回滚，无需人工介入

### DEP-02 【P2】docker-compose 补资源限制

- **端**：部署
- **文件**：`deploy/docker-compose.yml:4`（mysql）、`:25`（redis）、`:63`（algorithm）
- **现状**：三服务均无 CPU/内存限制，单容器暴涨可拖垮整机
- **改为**：各服务加 `deploy.resources.limits`（如 mysql 2G / redis 512M / algorithm 2G）
- **验收**：`docker stats` 中可见限额

### DEP-03 【P1】Nginx 安全与代理参数

- **端**：部署
- **文件**：`deploy/nginx/nginx.conf:73-79`
- **现状**：无安全响应头、无 `client_max_body_size`、无 `proxy_read_timeout`、无 `limit_req`
- **改为**：
  1. 补 `server_tokens off` + `X-Content-Type-Options` + `X-Frame-Options` + `Strict-Transport-Security`
  2. `client_max_body_size 20m`（当前默认 1MB 会阻断图片上传）
  3. `proxy_read_timeout 120s`、`proxy_connect_timeout 10s`
  4. 对 `/api/admin-api/member/auth/send-sms-code` 等加 `limit_req`
- **验收**：`curl -I` 可见安全头；上传 5MB 图片成功


### DEP-04 【P2】JVM 堆与算法服务并发

- **端**：部署
- **文件**：`yudao-server/Dockerfile`（`-Xmx512m`）、`algorithm/Dockerfile:30`（uvicorn 单 worker）
- **改为**：JVM `-Xmx2g`（按服务器内存调整）；算法服务评估多 worker 或保持单 worker 但明确排队策略
- **验收**：压测下无频繁 Full GC

### DEP-05 【P2】可观测补齐

- **端**：运维
- **文件**：`yudao-server/src/main/resources/logback-spring.xml:5,42,53`（traceId appender 被注释）；`scripts/monitor_watchdog.py:63-66,104-114`
- **改为**：
  1. 启用日志 traceId 输出（不依赖 SkyWalking Agent）
  2. watchdog 增加 webhook 外发告警；修正默认探测端口 `8000` → `18081`；注册为 systemd timer
- **验收**：日志可按 traceId 串联；服务异常能收到外部告警

---

## Wave 3 · UI 令牌下沉与缺陷修复

### 3A · 小程序

#### MP-01 【P0】冷启动必落登录页

- **端**：小程序
- **文件**：`app.json:2`（`pages[0]` = `pages/login/login`）、`pages/login/login.js:25-27`（无 token 判断）
- **现状**：已登录用户每次冷启动都要重新登录
- **改为**：见 Wave 4（与闪屏页合并实现）—— 在闪屏遮罩期做启动路由决策
- **依赖**：Wave 4

#### MP-02 【P1】令牌下沉 —— 重灾四页清零（答辩前置）

- **端**：小程序
- **文件与数量**：
  - `pages/driver/workbench/workbench.wxss`：**81 处**硬编码色 + 32 非令牌圆角 + 12 硬编码阴影
  - `pages/index/index.wxss`：31 处
  - `pages/parcel/parcel.wxss`：28 处
  - `pages/send/send.wxss`：25 处
- **现状**：353 处硬编码颜色 + 173 处非令牌圆角。默认主题切蓝后，切换主题会露出"半绿半蓝"
- **改为**：全部替换为 `var(--color-*)` / `var(--radius-*)` / `var(--shadow-*)`
- **验收**：在设置里依次切换 green/orange/blue/red 四套主题 + 深色模式，页面无残留绿色
- **备注**：**本 Wave 最耗时项**。建议按页分批，每页改完立刻切主题自查

#### MP-03 【P1】233 处小字号上调

- **端**：小程序
- **文件**：全项目（最集中：`parcel.wxss` 34 处、`workbench.wxss` 29 处、`send.wxss` 29 处、`index.wxss` 27 处）
- **现状**：`<28rpx` 共 233 处，最小 `20rpx`≈10px，低于微信建议最小值
- **改为**：按 1.2 节字号阶梯上调；`--font-caption`(24rpx) 仅允许用于票号/单位等非关键信息
- **验收**：`grep -rn "font-size: 2[0-7]rpx" pages/ app.wxss` 结果为空（caption 例外需注释说明）

#### MP-04 【P1】空态重试按钮不可见

- **端**：小程序
- **文件**：`pages/bus/index.wxml:79-81`、`pages/bus/detail.wxml:93-95`
- **现状**：`<view ... bindtap="loadNearby">` 包住 `<empty-state>`，文案写"点击重试"但没有按钮
- **改为**：改用 `empty-state` 的 `btnText="重新加载"`（其余 13 页已这么做）
- **验收**：两页错误态有可见重试按钮

#### MP-05 【P0】首页假空态闪烁

- **端**：小程序
- **文件**：`pages/index/index.wxml:92`（"暂无公告"）、`:261`（"暂无推荐农产品"）；`pages/index/index.js:33,43`
- **现状**：无 loading 判定，先渲染空态再突变为内容，弱网下"假空态"停留 2-3 秒
- **改为**：增加 `homeLoaded` 标志位，未加载完成时渲染骨架屏而非空态
- **验收**：弱网（3G）下不出现"暂无公告"闪变

#### MP-06 【P1】司机工作台无加载态（可能误导致操作）

- **端**：小程序
- **文件**：`pages/driver/workbench/workbench.wxml`（无任何骨架屏）、`:147`（`status === 'idle'` 分支直接渲染"待发车"面板）
- **现状**：`status` 默认值期间即渲染"待发车"，与真实排班无关，司机可能据此提前启动
- **改为**：加骨架屏；`status` 未就绪时不渲染任何操作面板
- **验收**：弱网下司机端先显示骨架，不出现"待发车"

#### MP-07 【P1】可点击区小于适老化基线

- **端**：小程序
- **文件**：约 30 处 `min-height: 80rpx` / `height: 84rpx` / `height: 76rpx`  
  （`index.wxss:58,96,157,431,554`、`orders.wxss:23,60`、`goods.wxss:58,138`、`parcel.wxss:133,204,443`、`bus/index.wxss:74,180,228`、`notification.wxss:34,52,257`、`send.wxss:408,538,594,771`、`workbench.wxss:481,650,773,1257` 等）
- **改为**：提升到 `min-height: 88rpx`（≈44px）
- **验收**：所有可点元素 ≥88rpx

#### MP-08 【P1】对比度修复

- **端**：小程序
- **文件**：
  - 空态副文案：`components/empty-state/index.wxss:13,18`（`#8A8778` on `#FFFFFF` = 3.63:1）→ 改 `var(--text-secondary)`
  - 占位符：`app.wxss:167-169`、`pages/send/send.wxss:73`（`#A6A6A6` on `#F7FAFD` = 2.35:1）→ 改 `#6B675C`
  - 金色底白图标：`pages/index/index.wxml:105`（白 on `#D9A441` = 2.25:1）→ 图标改深棕 `#5A4310`
  - 深色 tertiary：`utils/appearance.js:124`（`#8A8674` on `#24211B` = 4.39:1）→ 改 `#9C977F`
- **验收**：用对比度工具复核，全部 ≥4.5:1

#### MP-09 【P1】空态组件补 `btnText` 与主题色

- **端**：小程序
- **文件**：`components/empty-state/index.wxml:3`（`color="#C9C2B0"` 硬编码）、`index.wxss:29-31`（`#fff` + `999rpx`）
- **改为**：图标色改用 `appearance` 导出的 `iconColor`；`#fff` → `var(--color-on-primary)`；`999rpx` → `var(--radius-pill)`
- **验收**：切换主题后空态插图颜色跟随

#### MP-10 【P1】卡片与页头类名收敛

- **端**：小程序
- **文件**：18 个卡片类名（`info-card`/`desc-card`/`detail-card`/`overview-card`/`group-card`/`ho-card`/`route-card`/`addr-card`/`n-card`/`fb-card`/`carrier-card`/`proof-card`/`stops-card`/`product-card`/`weather-card`/`quote-card`/`reach-card`/`review-card`），9 份 `.top-bar` 复制，`.page-head` 与 `.page-header` 双写法
- **改为**：收敛为 `.card` / `.card-ticket` / `.page-head`（在 `app.wxss` 定义，页面引用）
- **验收**：卡片圆角/阴影/内边距全站一致；`grep -c "page-header" pages/**/*.wxss` 为 0

#### MP-11 【P1】自绘空态/错误横幅改组件

- **端**：小程序
- **文件**：`pages/index/index.wxml:148-182`（`.bus-empty`/`.bus-retry` 手写）、`pages/driver/workbench/workbench.wxml:51-55`（`load-error-banner` 手写）
- **改为**：统一用 `empty-state`
- **验收**：同屏不再出现两种空态长相

#### MP-12 【P2】补齐缺失状态

- **端**：小程序
- **文件**：`pages/mine/profile/profile.wxml`（无字段级 loading）、`pages/send/send.wxml`（无整页骨架）、`pages/index/index.js:319-321,329-331`（失败仅 `console.error`）
- **改为**：按 1.4 节组件状态规范补齐
- **验收**：每个列表/详情/表单页都有 loading / empty / error 三态

#### MP-13 【P2】404 图标补齐 + 语义拆分

- **端**：小程序
- **文件**：`components/icon/index.js:31-65`（32 个图标）；引用点见 1.3 节
- **改为**：新增 `caret-down`/`chevron-right`；拆分 `box`/`notice`/`sun`；`mine.wxml:35` 的 `home` 改 `user`
- **验收**：`grep -rn "▾\|›" pages/ components/` 为空

#### MP-14 【P2】`icon` 组件默认色硬编码

- **端**：小程序
- **文件**：`components/icon/index.js:79`（`value: '#2B2B28'`）
- **改为**：默认值改为读主题 `ink` 变量
- **验收**：深色模式下未传 color 的图标可读

#### MP-15 【P2】页面 json `backgroundColor` 硬编码

- **端**：小程序
- **文件**：`app.json:30` 及 8 个页面 json 均为 `"backgroundColor": "#F6F2E9"`
- **现状**：深色模式下下拉刷新会露出米黄色
- **改为**：改为 `#F5F8FC`（山泉蓝主题底）；深色模式需在 `app.json` 或用 `wx.setBackgroundColor` 动态切换
- **验收**：深色模式下下拉不露浅色

#### MP-16 【P2】首页无坐标的无效请求

- **端**：小程序
- **文件**：`pages/index/index.js:46-71`（`onLoad` 并行发 4 请求，`loadNearbyBusData()` 在定位回调前就执行）、`:380-382`
- **现状**：首次"附近公交"请求必然无坐标，弱网下多一个 RTT
- **改为**：`loadNearbyBusData` 延后到 `location.onLocationChange` 首次回调
- **验收**：Network 面板中首屏不再出现无坐标的 `bus/nearby` 请求

#### MP-17 【P2】`utils/toast.js` 统一封装

- **端**：小程序
- **文件**：约 80 处直调 `wx.showToast`（`send.js` 单页 14 处、`workbench.js` 20+ 处）
- **改为**：封装统一 duration/icon 参数（建议 duration 2500ms，适老化）
- **验收**：`grep -rn "wx.showToast" pages/` 仅剩 `utils/toast.js`

#### MP-18 【P2】老年模式覆写改为令牌驱动

- **端**：小程序
- **文件**：`app.wxss:520-550` + 各页 `elderly-mode` 块（`index.wxss:686-722`、`parcel.wxss:1009-1049`、`workbench.wxss:1198-1272`）
- **现状**：逐类打补丁，仍有 `20-24rpx` 文本未放大
- **改为**：改为令牌驱动的全局字号缩放（在 `page` 上覆盖字号类令牌）
- **验收**：开启老年模式后无 `<32rpx` 正文

#### MP-19 【P3】其他小程序项

- `pages/mine/mine.js:15` 的 `role` 死字段 + `mine.wxml:35-36` 写死"村民"
- `utils/config.js:14-20` develop 环境明文 HTTP（安全）
- `utils/api.js:78-81` 未登录仍发空 `Authorization` 头
- `utils/api.js:177,378` 两个死接口
- `README.md:23` 测试数量漂移（称 10 个实际 13 个）
- 补 `UpdateManager` 版本更新提示（当前完全没有）

---


### 3B · Web 管理端

#### WEB-01 【P0】回放页轨迹偏移约 500m

- **端**：Web 管理端
- **文件**：`src/views/transport/monitoring/replay.vue:162,181,220`
- **现状**：直接 `new BMapGL.Point(station.longitude, station.latitude)`，未做 GCJ-02 → BD-09 转换（`monitoring/index.vue:181` 做了）
- **改为**：调用 `@/components/Map/src/utils` 的 `gcj02ToBd09`，与监控页统一
- **顺带修正**：`replay.vue:84-85` 的过时注释（自述"待两页统一处理"）；`monitoring/index.vue:112-113` 的注释与代码相反
- **验收**：同一站点在监控页与回放页位置重合

#### WEB-02 【P1】建立 Web 令牌文件并接入

- **端**：Web 管理端
- **文件**：新建 `src/styles/tokens.scss`，在 `src/styles/index.scss:1` 处 `@use './tokens.scss';`
- **改为**：把第 1 部分的全部令牌落为 CSS 变量；`styles/variables.scss` 目前只有 `$namespace`，`theme.scss` 整文件被注释 —— 用 `tokens.scss` 替代
- **验收**：`grep -rn "var(--brand-\|var(--status-\|var(--surface-" src/views/transport/ | wc -l` > 100

#### WEB-03 【P1】163 处硬编码颜色替换

- **端**：Web 管理端
- **文件与数量**（`views/transport/`）：  
  `dispatch/DispatchVisualDialog.vue` **82 处**、`topology/index.vue` 24、`dispatch-center/index.vue` 22、`dashboard/index.vue` 13、`monitoring/index.vue` 12、`monitoring/replay.vue` 6，另有 4 处零散
- **改为**：全部替换为令牌变量；重点修 `dispatch-center/index.vue:289-291` 的地图图例、`monitoring/index.vue:282-285` 的车辆标签色
- **验收**：`grep -rn "#[0-9a-fA-F]\{6\}" src/views/transport/ | wc -l` 为 0（SVG 数据 URI 除外）

#### WEB-04 【P1】深色模式业务页适配

- **端**：Web 管理端
- **文件**：`src/styles/index.scss:57-64`（`.el-table th.el-table__cell { background-color: #f4f8f4; color: #2b2b28; }` 写死浅绿）；`src/views/transport/**` 内 `dark:` 出现 **0 次**
- **改为**：表头改 `var(--el-bg-color-overlay)` / `var(--el-text-color-primary)`；业务页补齐 `dark:` 适配
- **验收**：切换深色模式后无浅色亮块

#### WEB-05 【P1】低对比度文字全量替换

- **端**：Web 管理端
- **文件**：`#909399`（2.80:1）在 `route/RouteForm.vue:44`、`station/StationForm.vue:57`、`dispatch-center/index.vue:30`；`text-gray-400`（2.60:1）共 21 处（`order/index.vue:55,69,190,200,220,230`、`monitoring/index.vue:37,45,67,80`、`dashboard/index.vue:57,64,71,80` 等）
- **改为**：统一替换为 `text-gray-500` 或令牌 `var(--text-secondary)` 等价色
- **验收**：对比度全部 ≥4.5:1

#### WEB-06 【P1】长表单弹窗遮罩误关丢数据

- **端**：Web 管理端
- **文件**：`src/components/Dialog/src/Dialog.vue:77`（`close-on-click-modal="true"` 全局默认）
- **改为**：表单类弹窗置 `close-on-click-modal=false`；或在有改动时关闭前二次确认
- **验收**：填写长表单（如订单新增）时点遮罩不丢失数据

#### WEB-07 【P1】3 处绕过 Dialog 封装 + 弹窗宽度收敛

- **端**：Web 管理端
- **文件**：绕过点 `handover/index.vue:57`、`notification/index.vue:47`、`station/StationForm.vue:76`；宽度现有 11 种取值（550×8、500×4、720/480×2、1180/900/760/650/640/560/520）
- **改为**：3 处改回 `Dialog` 封装；宽度收敛为 3 档（480 / 720 / 1000px）
- **验收**：`grep -rn "<el-dialog" src/views/transport/` 为 0；`grep -o 'width="[0-9]*px"' | sort -u` 只剩 3 个值

#### WEB-08 【P1】术语统一 + 上下架枚举修正

- **端**：Web 管理端
- **文件**：
  - **上架/下架枚举反向**：`notice/index.vue:10-11`（上架=1）vs `product/index.vue:10-11` + `product/ProductForm.vue:61-62`（上架=0）
  - 站点：`station/index.vue:2`（站点）/ `dispatch/index.vue:180`（场站）/ `ProductOrderShipForm.vue:43`（网点）
  - 换乘点：`handover/index.vue:29`（换乘站）vs `DispatchVisualDialog.vue:773`（转接站）
  - 派单：`dispatch/index.vue:40,42`（派单）vs `:198`（智能调度）
- **改为**：建立 `views/transport/constants.ts` 单一来源表并全量替换；**上下架统一为 `1=上架`**（以公告为准，改商品侧）
- **验收**：`grep` 全项目无第二处硬编码枚举映射

#### WEB-09 【P1】5 处绕过 `constants.ts` 的状态映射

- **端**：Web 管理端
- **文件**：`vehicle/index.vue:63-64`（空闲/在途/故障/离线）与 `:32-33`（可用/停用）与 `constants.ts:67`（空闲/在途/停用）三者冲突；`DispatchVisualDialog.vue:436-437`（场站发车/乘客上车…）与 `constants.ts:45-47`（出发/接客…）冲突；`dispatch/index.vue:113-124`、`dashboard/index.vue:211`、`productOrder/index.vue:102-115`
- **改为**：抽取 `useStatusLabel()` 组合式函数，统一从 `constants.ts` 取
- **验收**：同一状态在全站显示同一文案

#### WEB-10 【P1】24 处静默 catch

- **端**：Web 管理端
- **文件**：示例 `order/index.vue:276-278`（`catch (e) { /* 兜底展示失败不影响订单列表 */ }`）
- **改为**：统一接入 `message.error` 或页面级错误横幅
- **验收**：接口失败时用户可见提示

#### WEB-11 【P1】空态补齐

- **端**：Web 管理端
- **文件**：全 transport 仅 4 处 `el-empty`（`dashboard/index.vue:107,148`、`dispatch-center/index.vue:42,88`）
- **改为**：所有列表页补 `el-empty` + 引导按钮
- **验收**：查无数据时表格有引导而非空白

#### WEB-12 【P1】地图坐标转换去重

- **端**：Web 管理端
- **文件**：`components/Map/src/utils.ts:73-86`（权威实现）+ `station/StationForm.vue:105-117`（重复）+ `dispatch/DispatchVisualDialog.vue:1152-1156`（重复）
- **改为**：统一引用 `utils.ts`，删除两处本地实现；`MapDialog.vue` 死代码决定激活或删除
- **验收**：`grep -rn "function gcj02ToBd09" src/` 只剩 1 处

#### WEB-13 【P2】地图默认中心不一致

- **端**：Web 管理端
- **文件**：`monitoring/index.vue:169`（重庆 `106.5765, 29.5325`）vs `monitoring/replay.vue:150`（成都 `104.0657, 30.5723`）
- **改为**：统一为重庆
- **验收**：无数据时两页初始视野一致

#### WEB-14 【P2】地图 marker 聚合

- **端**：Web 管理端
- **文件**：`monitoring/index.vue:183`（逐车 `addOverlay`）、`dispatch-center/index.vue:241-248`（站点 Circle + Label 全量叠加）
- **现状**：无聚合（全仓无 `MarkerClusterer`），叠加 10s 全量轮询
- **改为**：接入百度地图 `MarkerClusterer`；配合 BE-17 把轮询降到 30s
- **验收**：500 个 marker 时地图不卡顿

#### WEB-15 【P2】图表主题统一

- **端**：Web 管理端
- **文件**：`src/plugins/echarts/index.ts`（无 `registerTheme`）；3 处内联 option 在 `dashboard/index.vue:264,283,303`
- **改为**：注册 echarts 全局主题，收敛内联配色
- **验收**：新增图表可直接复用主题

#### WEB-16 【P2】表单校验补齐

- **端**：Web 管理端
- **文件**：`notification/index.vue:47-61,111-115`（`required` 只显示星号，实际靠 `message.warning` 手拦）、`handover/index.vue:57-62,119-123`（同型）
- **改为**：补 `el-form :rules` + 字段级错误提示
- **验收**：错误以字段级红字呈现，非 toast

#### WEB-17 【P2】危险操作补二次确认

- **端**：Web 管理端
- **文件**：`handover/index.vue:119-128`（标记争议无确认）、`route/RouteForm.vue:159-170`（保存站序会覆盖里程/时长，无确认）
- **改为**：补 `ElMessageBox.confirm`
- **验收**：两个操作均需确认

#### WEB-18 【P2】鼠标可点 div 补键盘可达性

- **端**：Web 管理端
- **文件**：`DispatchVisualDialog.vue:137-145,103,159-161`（面板 tab / 车辆图例 / route-card）、`monitoring/index.vue:71-72`（车辆列表项）
- **现状**：全仓 `tabindex`/`aria-`/`:focus-visible` 0 命中
- **改为**：补 `role="button"` + `tabindex="0"` + `@keydown.enter/space` + `:focus-visible` 样式
- **验收**：Tab 键可到达上述控件并触发

#### WEB-19 【P2】长文本列补 tooltip

- **端**：Web 管理端
- **文件**：`order/index.vue:66-77`（物品信息）、`driver/index.vue:24`（驾驶证号）、`route/index.vue:23`（线路名称）
- **改为**：加 `show-overflow-tooltip`
- **验收**：长文本可悬浮查看

#### WEB-20 【P2】5 个页面缺分页

- **端**：Web 管理端
- **文件**：`dashboard/index.vue`（结算表）、`dispatch-center/index.vue`（pageSize 写死 100）、`expiry/index.vue`（双表）、`ProductOrderDetail.vue`（弹窗内表，可豁免）、`topology/index.vue`
- **改为**：补 `<Pagination>`
- **验收**：数据量大时有翻页与总数

#### WEB-21 【P2】`dashboard` 的 loading 死状态

- **端**：Web 管理端
- **文件**：`dashboard/index.vue:213`（`const loading = ref(true)`）赋值但未在任何模板节点绑定；`:333,343-346` 全量失败仅 `console.error`
- **改为**：绑定 `v-loading` 并补错误态
- **验收**：加载中有遮罩，失败有提示

#### WEB-22 【P2】4 个页面「重置」不清空查询条件

- **端**：Web 管理端
- **文件**：`driver/index.vue:59`、`vehicle/index.vue:88`、`route/index.vue:80`、`shift/index.vue:64`
- **改为**：重置时清空全部查询字段
- **验收**：点重置后输入框与下拉均清空

#### WEB-23 【P2】风险操作禁用态与隐藏混用

- **端**：Web 管理端
- **文件**：`dispatch/index.vue:25,29-38`（`:disabled`）vs `order/index.vue:122`（`v-if` 隐藏）vs `dispatch/index.vue:97`（`:disabled` 但无原因提示）
- **改为**：统一用 `:disabled` + tooltip 说明原因
- **验收**：不可用操作可发现且能看到原因

#### WEB-24 【P2】ContentWrap 嵌套

- **端**：Web 管理端
- **文件**：`order/index.vue:2-3`、`dispatch/index.vue:2-4`
- **改为**：去掉内层 `ContentWrap`，改用普通容器
- **验收**：卡片无套卡片

#### WEB-25 【P2】响应式降级

- **端**：Web 管理端
- **文件**：`dispatch-center/index.vue:5,27,86`（固定 `:span="6/11/7"`）、`monitoring/index.vue:17`（`w-340px` 固定）、`DispatchVisualDialog.vue:1684`（`min-width: 420px`）
- **现状**：`views/transport` 内 `@media` 与 `sm:/md:/lg:` 均 0 次
- **改为**：补断点降级
- **验收**：1440px 以下无横向溢出

#### WEB-26 【P3】其他 Web 项

- `MarkdownView/index.vue:2` `v-html` 未显式净化（低风险）
- `directives/permission/hasPermi.ts:8-20` 类型错误时抛错但不移除元素（客户端可见）
- `order/index.vue:291-293` 删除后不修正页码
- `topology/index.vue:141-147`、`dispatch-center/index.vue:132-204` 无 try/catch 与 loading
- `vite.config.ts:82-87` `chunkSizeWarningLimit: 2000` 形同虚设
- `api/transport/index.ts:5` 幽灵接口 `/transport/test/get`（死代码，删除）
- 后端存在但前端未接入：`POST /transport/dispatch/allocate`、`GET /transport/monitoring/vehicle-plan`
- 业务页 i18n 或明确放弃（当前 `useI18n` 在 `views/transport` 0 命中）

---

## Wave 4 · 小程序闪屏页与启动链路

> **实现方案：首页内嵌遮罩层（不新增页面）。**  
> 理由：微信客户端启动图不可编程；新增独立 `pages/splash` 会多一次页面栈切换且自身也要 `appearance.apply`，闪色问题依旧。

### SPL-01 【P0】闪屏遮罩层实现

- **端**：小程序
- **文件**：`pages/index/index.wxml`（新增遮罩层）、`pages/index/index.wxss`（新增样式）、`pages/index/index.js`（就绪判定）
- **规格**：
  ```
  画布     750 × 1334rpx 全屏，z-index 9999，position fixed，覆盖 tabBar（高 100rpx）
  背景     linear-gradient(160deg,#123F6E 0%,#1F5E9E 58%,#2E7BBF 100%)
           + 底部山峦剪影 SVG（两层错位，#fff 7% 与 #000 16%）
           + 中央径向光晕 rgba(255,255,255,0.08)，直径 900rpx
  站牌Logo 主体 320×200rpx，--radius-l，rgba(255,255,255,0.14)
           顶部横档 280×12rpx，--radius-pill，#D9A441 稻谷金
           中央 icon name="bus" size=96 color="#FFFFFF"
           上浮动画 translateY(0→-8rpx→0)，2.4s ease-in-out 无限循环
  主标题   「农村客货邮」48rpx/700/#FFFFFF/letter-spacing 4rpx
  副标题   「公交带货 · 快递进村 · 农产品出山」26rpx/400/rgba(255,255,255,0.82)
  进度条   480×8rpx，轨道 rgba(255,255,255,0.20)，填充 稻谷金→亮蓝 #E8B855→#6FB3E8
           宽度由真实阶段驱动（0/33/66/100%），不做假动画
  状态文案 24rpx/rgba(255,255,255,0.72)
           「正在连接服务站…」→「正在准备您的班次…」→「马上就好…」
  跳过按钮 右下角，距底 120rpx/距右 32rpx，88×88rpx 圆形（适老化基线）
           文字「跳过」26rpx + rgba(255,255,255,0.72) + 1rpx 描边 rgba(255,255,255,0.32)
  版本号   距底 48rpx，22rpx/rgba(255,255,255,0.45)
  ```

### SPL-02 【P0】时序与兜底

- **端**：小程序
- **参数**：
  | 参数   | 值                                          | 说明                           |
  | ---- | ------------------------------------------ | ---------------------------- |
  | 最短展示 | **800ms**                                  | 低于此会"闪一下"，比不做还差              |
  | 最长封顶 | **2500ms**                                 | 弱网必须封顶，否则像卡死                 |
  | 淡出   | opacity 1→0，300ms ease-out，结束后 `v-if` 移除节点 | 释放渲染                         |
  | 触发   | 任一请求失败 → 照常淡出 + 首页顶部可重试横幅                  | 参考 `workbench.wxml:51` 的横幅模式 |
  | 二次进入 | 用 `globalData` 标记，本次生命周期已展示则不再展示           | 避免 `switchTab` 返回时重现         |
- **验收**：断网/弱网下遮罩均 ≤2.5s 结束，页面可交互

### SPL-03 【P0】启动路由决策（顺带修 MP-01）

- **端**：小程序
- **文件**：`pages/index/index.js`（遮罩期）、`pages/login/login.js`
- **逻辑**：
  ```
  遮罩期并行执行（Promise.allSettled）：
    ① 校验本地 token 有效性
    ② 拉取用户信息 getUserInfo
    ③ 首屏数据预取（复用 index.onLoad 已有请求，避免二次请求）
    ④ 检查小程序版本更新（UpdateManager）
  分支：
    token 有效 + 用户信息成功 → 淡出 → 首页内容已就绪
    token 无效/过期          → 淡出 → 进首页，不硬跳登录（要用再登）
    网络失败                 → 淡出 → 进首页 + 顶部可重试横幅
  ```
- **改为**：`app.json` 的 `pages[0]` 由 `pages/login/login` 改为 `pages/index/index`；`utils/auth.js:22-42` 的 `requireLogin` + `loginRedirect` 机制复用
- **验收**：已登录用户冷启动 ≤2.5s 直接看到首页内容，**不需要重新登录**

### SPL-04 【P1】`app.js onLaunch` 补初始化

- **端**：小程序
- **文件**：`app.js:1-13`（当前 onLaunch 为空）
- **改为**：读缓存主题/深色/大字 → 立即 `appearance.apply()`；补 `globalData.darkMode` 字段；`currentVillage` 的硬编码 `'云山村'` 改为从缓存读
- **验收**：首帧即正确主题色，无颜色跳变

### SPL-05 【P1】闪屏期消除首屏"假空态"

- **端**：小程序
- **依赖**：MP-05
- **验收**：闪屏淡出后不出现"暂无公告"突变

---

## Wave 5 · 短信验证码（测试环境先恢复可用）

> **根因已确证**：登录场景 scene=1 → 模板 `user-sms-login` 绑定在 `DEBUG_DING_TALK` 渠道（`system_sms_template` id=8, channel_id=4）。该渠道的 `DebugDingTalkSmsClient` **不发短信，只往钉钉机器人推消息**。  
> 铁证：`system_sms_log` id=1570 `send_status=10`（成功）但手机无短信。  
> 次因：三个渠道凭据全是 yudao 上游演示值，项目从未接入真实服务商。

### SMS-01 【P1】测试环境：非生产 profile 固定验证码

- **端**：后端配置
- **文件**：`yudao-server/src/main/resources/application-dev.yaml`、`application-local.yaml`（**当前两者都没有 `sms-code` 段**）
- **现状**：`application.yaml:190-191` 的注释写"本地测试可经 application-dev.yaml 覆盖为固定值"，但实际覆盖不存在 —— 注释与实现不一致
- **改为**：在**非生产** profile 增加：
  ```yaml
  yudao:
    sms-code:
      begin-code: 8888
      end-code: 8888
  ```
- **验收**：测试环境任意手机号验证码恒为 `8888`（≤6 位，匹配 `pages/login/login.wxml:47` 的 `maxlength="6"`）
- **⚠️ 禁止**：不要改 `application.yaml`（生产）的 `begin-code: 100000 / end-code: 999999`

### SMS-02 【P1】消除静默失败（管理端展示发送结果）

- **端**：后端 + Web 管理端
- **文件**：`yudao-module-system/.../service/sms/SmsSendServiceImpl.java:81-105,158-174`（异步消费者写好失败结果到 `system_sms_log`）；管理端短信日志页
- **现状**：`sendSingleSms` 同步只做"校验+建日志+发事件"，真正发送在 `@Async SmsSendConsumer`；失败只更新日志表，**Controller 恒返回 `success=true`**
- **改为**：管理端「短信日志」列表显式展示 `send_status` / `api_send_code` / `api_send_msg`；对 `send_status=20`（失败）增加告警
- **验收**：不查数据库即可看到每条的发送结果与失败原因

### SMS-03 【人工 · AI 不可完成】接入真实短信服务商

- **前置**：开通阿里云/腾讯云短信，申请**签名**与**模板**（需企业资质，约 1~2 工作日审核）
- **步骤**：
  1. 管理端「系统管理 → 短信渠道」新建渠道，填真实 AK/SK，`status=启用`
  2. 「短信模板」新建 `code=user-sms-login`，渠道选新渠道，`api_template_id` 填服务商模板 ID，变量必须是 `{code}`
  3. 备份后删除/改码旧的 `user-sms-login` 行（避免同 code 冲突）
  4. 手机号真实收发验证
- **⚠️ 安全**：不要复用现有演示凭据（`channel id=2` 是 yudao 作者 Ballcat 的账号，remark 原文"你要改哦，只有我可以用！！！！"）。且该钉钉 WebHook 曾返回成功，意味着测试验证码可能被推送到**第三方钉钉群** —— 存在泄露风险，应尽快停用该渠道

### SMS-04 【P2】限流参数确认

- **端**：后端
- **文件**：`controller/app/auth/AppAuthController.java:79,87`（`@RateLimiter(time=60, count=5)` 按 IP）；`application.yaml:188-189`（`send-frequency: 1m`、日上限 10）
- **说明**：现状可接受，但需知道**入口限流拒绝时连 code/log 都不产生**，排障时容易误判"请求没到后端"。建议在管理端短信日志页加"被限流次数"统计
- **验收**：限流行为在监控中可见

---

## Wave 6 · 智慧大屏

> **⚠️ 数据层铁律**：`GET /transport/monitoring/vehicles` 是 **6 张表全表扫描 + 每车 N+1**（`MonitoringServiceImpl.java:150-163,227-230`），而 DB 连接池仅 `max-active: 20`。  
> 若按"6 模块 × 5s 轮询"实现 = 72 req/min，**会直接把后端打挂**（与 BE-02 同一条链路）。  
> **必须先做 SEP-01 + BE-02，再做视觉层。**

### SEP-01 【P0】大屏聚合接口 + 缓存（数据层前置）

- **端**：后端
- **改为**：
  1. 新增 `GET /transport/bigscreen/overview`，一次返回 T2 层全部模块（摘要 KPI + 今日班次 + 返程结算 + 订单趋势）
  2. 为 `bigscreen/overview` 与 `monitoring/vehicles` 加 Redis 缓存，TTL = 刷新间隔 × 0.8（如 50s / 12s）
- **效果**：把「N 个观看者 × 5 个接口」收敛为「1 个接口 + 1 次 DB 查询」，大屏并发压力降到与 1 个用户相同
- **验收**：10 个客户端同时打开大屏，DB QPS 与单个客户端相同

### SEP-02 【P0】订单趋势支持小时粒度

- **端**：后端
- **文件**：`controller/admin/dashboard/DashboardController.java:118`（`DATE_FORMAT(create_time,'%Y-%m-%d')` 仅到日）
- **改为**：`order-statistics` 增加 `granularity` 参数（`hour` 时用 `'%Y-%m-%d %H'`）
- **验收**：`GET /admin-api/transport/dashboard/order-statistics?granularity=hour` 返回 24 个点
- **备注**：**不改表即可实现，性价比最高**

### SEP-03 【P1】大屏视觉层

- **端**：Web 管理端
- **新文件**：`src/views/transport/bigscreen/index.vue`；路由 `/transport/bigscreen`，`meta: { hidden: true, noTagsView: true }` 全屏无菜单
- **画布**：`1920 × 1080` 固定设计稿 + `transform: scale(min(vw/1920, vh/1080))` 等比缩放，`transform-origin: 0 0`
- **配色（山乡夜航 · 山泉蓝调，拒绝 DataV 霓虹青紫）**：
  ```
  背景       #0B1220   深蓝墨
  背景层次   #111B2B   面板底
  面板描边   #1D2C42
  网格/分隔  #23364F
  主色       #4F93D6   山泉蓝提亮（深底保证对比度）
  主色发光   rgba(79,147,214,0.28)
  数据金     #E8B855   稻谷金（KPI 高亮）
  行动橙     #E07A3F   陶土橙（告警）
  中性文字   #E6EDF5 / #93A5BC（次级）
  成功/在线  #4CAF50   绿（与主色蓝区分）
  离线/停用  #6B7A73   中性灰
  ```
- **结构（四区）**：
  ```
  ① 顶栏 h=88   标题 + 日期时间 + 最后更新时间 + 全屏切换
  ② 左栏 w=440  运营 KPI 6 项 / 订单营收 24h 双轴趋势 / 订单类型分布
  ③ 中栏 w=968  全域运行地图（站点+线路+车辆）+ 底部 KPI 条 4 项
  ④ 右栏 w=440  今日班次执行 / 运力实时状态 / 异常与待办
  ⑤ 底栏 h=56   滚动事件流
  ```
- **规范**：面板 `#121E19→#111B2B` 底 + 1px 描边 + 圆角 16px + 内边距 20px + 左侧 3px 主色竖条；KPI 数字 40px/700/`tabular-nums`；**禁止无限循环动画**（7×24 运行）；某模块失败时显示"数据延迟 xm" + 橙色圆点
- **刷新分层**：T1 静态 5min（map-data / statistics）/ T2 中频 60s（summary / order-statistics / shift-execution / settlement）/ T3 高频 15s（vehicles）
- **验收**：1920×1080、2560×1440、4K 下均正常显示且 16:9 等比

### SEP-04 【P1】echarts 大屏主题注册

- **端**：Web 管理端
- **文件**：`src/plugins/echarts/index.ts`（当前无 `registerTheme`；若要省市地图还需补 `GeoComponent`）
- **改为**：注册大屏暗色主题
- **验收**：大屏图表无需逐图配置配色

### SEP-05 【P2】地图 marker 聚合 + 暗色底图

- **端**：Web 管理端
- **依赖**：WEB-14
- **改为**：接入 `MarkerClusterer`；百度地图 GL 配置暗色样式（或叠加半透明遮罩）
- **验收**：500+ marker 不卡顿；底图为暗色

### SEP-06 【P2】缺失数据如实标注

- **端**：Web 管理端
- **说明**：以下大屏模块**当前无数据源，一期禁止伪造**，UI 上标注"建设中"：
  | 数据项        | 现状                                            |
  | ---------- | --------------------------------------------- |
  | 实时告警流      | 无告警表无接口 → 一期用"交接争议 + 证件到期"组合替代                |
  | 算法 QPS/P99 | `transport_algorithm_request` **无耗时字段**，且无读接口 |
  | 司机在线率      | 司机表无在线字段 → 用"今日有执行记录的司机/司机总数"近似               |
  | 车辆 GPS 热力  | 无跨车聚合接口（单车单次上限 2000 点）                        |
  | 站点作业热力     | 仅调度校验时返回入参相关                                  |
- **验收**：大屏不出现编造的假数据

---

## Wave 7 · 工程与测试补齐

### ENG-01 【P1】抢单/装车/取消 补真实并发测试

- **端**：测试
- **文件**：`yudao-module-transport/src/test/.../dispatch/DispatchServiceImplTest.java:342`（现仅 `when(update).thenReturn(0)` 桩模拟）
- **现状**：全仓无真实多线程测试（`grep ExecutorService|CountDownLatch|Thread(` 仅命中 1 个方法名）
- **改为**：用 `CountDownLatch` + 真实 DB（或 Testcontainers）覆盖：抢单、装车超载、订单取消补库存
- **验收**：≥6 个并发场景用例

### ENG-02 【P1】鉴权拦截补测试

- **端**：测试
- **文件**：`yudao-framework/yudao-spring-boot-starter-security/src/test/`（**目录为空**）
- **改为**：补令牌伪造、过期、越权访问、`/admin-api` 与 `/app-api` 拦截边界用例
- **验收**：≥4 个用例

### ENG-03 【P1】事务回滚改真实验证

- **端**：测试
- **文件**：`DispatchServiceImplTest.java:202,231,252`（现全部依赖 `verify(mapper)` 调用次数）
- **改为**：改为真实事务提交/回滚断言
- **验收**：改坏 `@Transactional` 传播行为后测试会失败

### ENG-04 【P1】前端接入 vitest

- **端**：Web 管理端
- **文件**：`yudao-ui/yudao-ui-admin-vue3/package.json`（无 vitest/jest）
- **改为**：接入 vitest，先覆盖 `src/utils/` 纯函数与 `src/api/` 契约
- **验收**：CI 增加 `pnpm test` 门禁

### ENG-05 【P1】CI 补 lint 与静态扫描

- **端**：CI
- **文件**：`.github/workflows/ci.yml:62-65`（仅 `mvn test` + `package`，无 lint）
- **改为**：接入 `scripts/scan_java_issues.py` 与前端 eslint；增加**硬编码颜色门禁**（新增样式出现 hex 字面量即失败）
- **验收**：CI 能拦住新的硬编码颜色

### ENG-06 【P2】CI 健壮性

- **端**：CI
- **文件**：`ci.yml:44-49`（路径正则脆弱）、无 `concurrency`；`deploy-dev.yml:136-142`（部署阶段 `-DskipTests`）
- **改为**：加 `concurrency`；路径检测改为显式枚举；手动触发部署时强制跑测试
- **验收**：连续 push 自动取消旧 job

### ENG-07 【P3】工程卫生

- `.pytest_cache`（根、`algorithm/`、`mock-algorithm/`）加入 `.gitignore`
- 上游模块 22 处 `@Disabled` 用例：标注原因或移出 CI 测试树
- 修 `README.md:23` 测试数量漂移

---

# 第 3 部分 · 任务索引（执行进度勾选表）

| ID     | 严重度 | Wave | 端      | 文件                                          | 完成 |
| ------ | --- | ---- | ------ | ------------------------------------------- | -- |
| CFG-01 | P0  | 0    | Web    | `store/modules/app.ts:77`                   | ☑  |
| CFG-02 | P0  | 0    | Web    | `Setting/src/Setting.vue:218`               | ☑  |
| CFG-03 | P0  | 0    | Web    | `styles/index.scss:39`                      | ☑  |
| CFG-04 | P1  | 0    | Web    | `styles/var.css:49`                         | ☑  |
| CFG-05 | P1  | 0    | 小程序    | `app.json:27` / `app.wxss:14` / `app.js:11` | ☑  |
| CFG-06 | P1  | 0    | 小程序    | `custom-tab-bar/index.js:41`                | ☑  |
| CFG-07 | P2  | 0    | 小程序    | 9 处 `confirmColor`                          | ☑  |
| BE-01  | P0  | 1    | 后端     | `AlgorithmProperties.java:24`               | ☑  |
| BE-02  | P0  | 1    | 后端     | `application-local.yaml:34`                 | ☑  |
| BE-03  | P0  | 1    | Web/仓库 | 6 个 `.env*`                                 | ☐  |
| BE-04  | P1  | 1    | 后端     | `TransportOrderServiceImpl.java:92`         | ☑  |
| BE-05  | P1  | 1    | 后端     | `DispatchServiceImpl.java:401`              | ☑  |
| BE-06  | P1  | 1    | 后端     | `DriverAppServiceImpl.java:894`             | ☑  |
| BE-07  | P1  | 1    | 后端     | `ProductOrderServiceImpl.java:131`          | ☑  |
| BE-08  | P1  | 1    | 后端     | `TransportOrderServiceImpl.java:141`        | ☑  |
| BE-09  | P1  | 1    | 后端     | `AppDriverController.java:247`              | ☑  |
| BE-10  | P1  | 1    | 后端     | 全模块 `@DataPermission`                       | ☐  |
| BE-11  | P1  | 1    | 后端     | `AppSendController.java:482`                | ☑  |
| BE-12  | P1  | 1    | 后端     | `SecurityConfiguration.java:30`             | ☑  |
| BE-13  | P1  | 1    | 后端     | `DispatchServiceImpl.java:1244`             | ☑  |
| BE-14  | P1  | 1    | 后端     | `MultiLegServiceImpl.java:188`              | ☑  |
| BE-15  | P1  | 1    | 后端     | `LegConflictServiceImpl.java:92`            | ☑  |
| BE-16  | P1  | 1    | 后端     | `StationServiceImpl.java:45`                | ☑  |
| BE-17  | P1  | 1    | 后端+Web | `DashboardController.java:93`               | ☑  |
| BE-18  | P1  | 1    | DB     | 索引迁移脚本                                      | ☑  |
| BE-19  | P1  | 1    | 后端     | `GlobalExceptionHandler.java:351`           | ☑  |
| BE-20  | P1  | 1    | 后端     | `YudaoAsyncAutoConfiguration.java:15`       | ☑  |
| BE-21  | P2  | 1    | 后端     | `DriverAppServiceImpl.java:1411`            | ☑  |
| BE-22  | P2  | 1    | 后端     | `MultiLegServiceImpl.java:696`              | ☑  |
| BE-23  | P2  | 1    | 后端     | `DispatchServiceImpl.java:3739`             | ☑  |
| BE-24  | P2  | 1    | DB     | `V019:152`                                  | ☑  |
| BE-25  | P2  | 1    | DB     | `transport-schema.sql:353`                  | ☑  |
| BE-26  | P2  | 1    | 后端     | `AppSendController.java:127`                | ☑  |
| BE-27  | P2  | 1    | 后端     | `DispatchServiceImpl.java:549`              | ☑  |
| BE-28  | P2  | 1    | 后端     | `UserNotificationServiceImpl.java:58`       | ☑  |
| BE-29  | P2  | 1    | 后端     | `AlgorithmAdapter.java:55`                  | ☑  |
| BE-30  | P3  | 1    | 后端     | 39 处 `@Transactional`                       | ☑  |
| BE-31  | P3  | 1    | 后端     | `MultiLegServiceImpl.java:160`              | ☑  |
| DEP-01 | P1  | 2    | 部署     | `deploy-dev.yml:661`                        | ☑  |
| DEP-02 | P2  | 2    | 部署     | `docker-compose.yml:4,25,63`                | ☑  |
| DEP-03 | P1  | 2    | 部署     | `nginx/nginx.conf:73`                       | ☑  |
| DEP-04 | P2  | 2    | 部署     | `Dockerfile`                                | ☑  |
| DEP-05 | P2  | 2    | 运维     | `logback-spring.xml:42`                     | ☑  |
| MP-01  | P0  | 3→4  | 小程序    | `app.json:2`                                | ☑  |
| MP-02  | P1  | 3    | 小程序    | 重灾四页                                        | ☑  |
| MP-03  | P1  | 3    | 小程序    | 233 处字号                                     | ☑  |
| MP-04  | P1  | 3    | 小程序    | `bus/index.wxml:79`                         | ☑  |
| MP-05  | P0  | 3    | 小程序    | `index.wxml:92,261`                         | ☑  |
| MP-06  | P1  | 3    | 小程序    | `workbench.wxml:147`                        | ☑  |
| MP-07  | P1  | 3    | 小程序    | ~30 处点击区                                    | ☑  |
| MP-08  | P1  | 3    | 小程序    | 对比度 4 处                                     | ☑  |
| MP-09  | P1  | 3    | 小程序    | `empty-state/*`                             | ☑  |
| MP-10  | P1  | 3    | 小程序    | 18 卡片类 + 9 份 page-head                      | ☑  |
| MP-11  | P1  | 3    | 小程序    | `index.wxml:148`、`workbench.wxml:51`        | ☑  |
| MP-12  | P2  | 3    | 小程序    | profile/send/index                          | ☑  |
| MP-13  | P2  | 3    | 小程序    | `components/icon/index.js`                  | ☑  |
| MP-14  | P2  | 3    | 小程序    | `components/icon/index.js:79`               | ☑  |
| MP-15  | P2  | 3    | 小程序    | 9 个 json `backgroundColor`                  | ☑  |
| MP-16  | P2  | 3    | 小程序    | `pages/index/index.js:46`                   | ☑  |
| MP-17  | P2  | 3    | 小程序    | ~80 处 `showToast`                           | ☑  |
| MP-18  | P2  | 3    | 小程序    | `app.wxss:520`                              | ☑  |
| MP-19  | P3  | 3    | 小程序    | 6 项                                         | ☑  |
| WEB-01 | P0  | 3    | Web    | `replay.vue:162`                            | ☑  |
| WEB-02 | P1  | 3    | Web    | 新建 `styles/tokens.scss`                     | ☑  |
| WEB-03 | P1  | 3    | Web    | 163 处硬编码色                                   | ☑  |
| WEB-04 | P1  | 3    | Web    | `styles/index.scss:57`                      | ☑  |
| WEB-05 | P1  | 3    | Web    | 21 处低对比度                                    | ☑  |
| WEB-06 | P1  | 3    | Web    | `Dialog/src/Dialog.vue:77`                  | ☑  |
| WEB-07 | P1  | 3    | Web    | 3 处 + 11 种宽度                                | ☑  |
| WEB-08 | P1  | 3    | Web    | 术语 + 上下架枚举                                  | ☑  |
| WEB-09 | P1  | 3    | Web    | 5 处状态映射                                     | ☑  |
| WEB-10 | P1  | 3    | Web    | 24 处静默 catch                                | ☑  |
| WEB-11 | P1  | 3    | Web    | 空态                                          | ☑  |
| WEB-12 | P1  | 3    | Web    | 坐标转换 3→1                                    | ☑  |
| WEB-13 | P2  | 3    | Web    | 地图中心                                        | ☑  |
| WEB-14 | P2  | 3    | Web    | marker 聚合                                   | ☑  |
| WEB-15 | P2  | 3    | Web    | echarts 主题                                  | ☑  |
| WEB-16 | P2  | 3    | Web    | 2 处表单校验                                     | ☑  |
| WEB-17 | P2  | 3    | Web    | 2 处二次确认                                     | ☑  |
| WEB-18 | P2  | 3    | Web    | 键盘可达性                                       | ☑  |
| WEB-19 | P2  | 3    | Web    | 长文本 tooltip                                 | ☑  |
| WEB-20 | P2  | 3    | Web    | 5 页缺分页                                      | ☑  |
| WEB-21 | P2  | 3    | Web    | `dashboard/index.vue:213`                   | ☑  |
| WEB-22 | P2  | 3    | Web    | 4 页重置                                       | ☑  |
| WEB-23 | P2  | 3    | Web    | 禁用态                                         | ☑  |
| WEB-24 | P2  | 3    | Web    | ContentWrap 嵌套                              | ☑  |
| WEB-25 | P2  | 3    | Web    | 响应式                                         | ☑  |
| WEB-26 | P3  | 3    | Web    | 8 项                                         | ☑  |
| SPL-01 | P0  | 4    | 小程序    | `index.wxml/wxss/js`                        | ☑  |
| SPL-02 | P0  | 4    | 小程序    | 时序兜底                                        | ☑  |
| SPL-03 | P0  | 4    | 小程序    | 启动路由决策                                      | ☑  |
| SPL-04 | P1  | 4    | 小程序    | `app.js:1`                                  | ☑  |
| SPL-05 | P1  | 4    | 小程序    | 依赖 MP-05                                    | ☑  |
| SMS-01 | P1  | 5    | 后端配置   | `application-dev.yaml`                      | ☑  |
| SMS-02 | P1  | 5    | 后端+Web | 短信日志页                                       | ☑  |
| SMS-03 | —   | 5    | 人工     | 控制台配置                                       | ☐  |
| SMS-04 | P2  | 5    | 后端     | 限流观测                                        | ☑  |
| SEP-01 | P0  | 6    | 后端     | 聚合接口 + 缓存                                   | ☑  |
| SEP-02 | P0  | 6    | 后端     | `DashboardController.java:118`              | ☑  |
| SEP-03 | P1  | 6    | Web    | 新建 `bigscreen/index.vue`                    | ☑  |
| SEP-04 | P1  | 6    | Web    | `plugins/echarts/index.ts`                  | ☑  |
| SEP-05 | P2  | 6    | Web    | 依赖 WEB-14                                   | ☑  |
| SEP-06 | P2  | 6    | Web    | 缺失数据标注                                      | ☑  |
| ENG-01 | P1  | 7    | 测试     | `DispatchServiceImplTest.java:342`          | ☐  |
| ENG-02 | P1  | 7    | 测试     | security starter test                       | ☑  |
| ENG-03 | P1  | 7    | 测试     | `DispatchServiceImplTest.java:202`          | ☐  |
| ENG-04 | P1  | 7    | Web    | `package.json`                              | ☑  |
| ENG-05 | P1  | 7    | CI     | `ci.yml:62`                                 | ☑  |
| ENG-06 | P2  | 7    | CI     | `ci.yml:44`                                 | ☑  |
| ENG-07 | P3  | 7    | 工程     | 3 项                                         | ☑  |

**合计 110 项**：P0 = 14、P1 = 53、P2 = 37、P3 = 5，另含 1 项人工任务（SMS-03，标记为 `—`）

---

# 第 4 部分 · 验证命令集

```bash
P=/c/Users/袁/Desktop/测试/cargo-post-platform
MP=/c/Users/袁/Desktop/测试/cargo-post-miniprogram

# ── Wave 0：配色对齐 ──
grep -rn "2e7d32" $P/yudao-ui/yudao-ui-admin-vue3/src/          # 应仅剩 index.scss 成功色 1 处
grep -rn "409eff" $P/yudao-ui/yudao-ui-admin-vue3/src/layout/components/Setting/   # 应为空
grep -n "1C4B2E\|#2E7D32" $MP/app.json $MP/app.js               # 应为空（或仅注释）

# ── Wave 1：后端测试 ──
cd $P && mvn -B -pl yudao-server -am clean test
grep -rn "@DataPermission" --include=*.java $P/yudao-module-transport | wc -l   # 应 > 0
grep -rn "@Idempotent"     --include=*.java $P/yudao-module-transport | wc -l   # 应 > 0
git ls-files | grep -E '(^|/)\.env$'                            # 应为空

# ── Wave 3：UI 一致性门禁 ──
# 小程序硬编码颜色（目标：重灾四页为 0）
for f in workbench index parcel send; do
  echo -n "$f: "; grep -oE "#[0-9a-fA-F]{3,6}" $MP/pages/**/$f*.wxss 2>/dev/null | wc -l
done
# 小程序小字号（目标 <28rpx 接近 0）
grep -rn "font-size: 2[0-7]rpx" $MP/pages $MP/app.wxss | wc -l
# 文本字形图标（目标 0）
grep -rn "▾\|›" $MP/pages $MP/components | wc -l
# Web 硬编码颜色（目标 0）
grep -rn "#[0-9a-fA-F]\{6\}" $P/yudao-ui/yudao-ui-admin-vue3/src/views/transport/ | wc -l
# Web 原生 el-dialog（目标 0）
grep -rn "<el-dialog" $P/yudao-ui/yudao-ui-admin-vue3/src/views/transport/ | wc -l

# ── Wave 4：闪屏页验收 ──
# 微信开发者工具 → 网络限速 3G → 冷启动，观察遮罩 ≤2.5s 结束
# 已登录态冷启动：应直接进首页，不出现登录页

# ── Wave 5：短信 ──
# 测试环境发码，验证码应为 8888
curl -s -X POST "http://1.15.29.107/api/app-api/member/auth/send-sms-code" \
  -H "Content-Type: application/json" -d '{"mobile":"13800138000","scene":1}'
# 管理端短信日志应显示 send_status=10 且能看到 api_send_msg

# ── Wave 6：大屏数据层 ──
# 10 个客户端同时打开大屏，观察 DB 连接数不应超过单客户端水平
# EXPLAIN 验证 SEP-02 的小时粒度查询
```

---

# 第 5 部分 · 不改清单（防过度改动）

| #  | 不要动                                                                    | 理由                                                                |
| -- | ---------------------------------------------------------------------- | ----------------------------------------------------------------- |
| 1  | `utils/appearance.js:19` 的 `DEFAULT_THEME = 'blue'`                    | **已确认是国赛答辩要求**，保持不动                                               |
| 2  | 小程序 4 套主题定义 `THEMES`（green/orange/blue/red）                            | 保留多主题能力，只改默认值与令牌引用                                                |
| 3  | `application.yaml:190-191` 的 `begin-code: 100000 / end-code: 999999`   | 生产随机码区间，只在非生产 profile 覆盖                                          |
| 4  | `styles/index.scss:42` 的 `--el-color-success: #2e7d32`                 | 主色转蓝后它已成为独立的成功色，**保留**                                            |
| 5  | 已正确的 CAS 实现                                                            | 运输段推进、交接开始/确认/超时、承运审核、确认送站、司机装车/妥投/核销、库存扣减 —— 这些**已经用了 CAS**，不要重写 |
| 6  | admin 端 `@PreAuthorize` 注解                                             | 已有写接口 **100% 覆盖**，是本项目质量亮点                                        |
| 7  | `deploy/docker-compose.yml` 的 `127.0.0.1:` 端口绑定                        | 已正确防止数据库/Redis 暴露公网                                               |
| 8  | `.github/workflows/ci.yml:191-197` 的 `.env` 拒绝跟踪门禁                     | 正向机制，**只扩大范围不要删**                                                 |
| 9  | `docker-compose.yml` 各服务的 `healthcheck` 与 `restart` 策略                 | 已配置完善                                                             |
| 10 | `deploy/nginx/nginx.conf` 的 `/assets/` 长缓存与 `index.html` no-cache      | 有实战故障注释支撑，不要改                                                     |
| 11 | 小程序的 `empty-state` / `icon` / `station-picker` / `driver-tab-bar` 组件结构 | 组件本身设计合理，只改内部色值引用                                                 |
| 12 | `views/transport/constants.ts`                                         | 它已经是正确做法，**补全它**而不是绕过它                                            |

---

# 第 6 部分 · 人工任务（AI 无法完成）

| # | 任务              | 说明                                                               |
| - | --------------- | ---------------------------------------------------------------- |
| 1 | **轮换已泄露凭据**     | admin 密码、百度地图 AK、AES 密钥已进入 Git 历史，`git rm --cached` 无法消除历史泄露     |
| 2 | **接入真实短信服务商**   | 需企业资质报备签名与模板（约 1~2 工作日），见 SMS-03                                 |
| 3 | **确认区域→站点归属模型** | BE-10 数据权限落地前需业务确认权限维度                                           |
| 4 | **停用钉钉演示渠道**    | `DEBUG_DING_TALK`（channel id=4）的 WebHook 曾返回成功，测试验证码可能被推送到第三方钉钉群 |
| 5 | **确认服务器资源**     | BE-02 / DEP-04 的扩容上限取决于服务器实际内存                                   |
| 6 | **分支保护规则核对**    | 确认 CI backend/frontend job 已设为必需检查（GitHub 仓库设置，代码外）              |
| 7 | ~~百度地图暗色底图配置~~  | ✅ 已改为 SDK 端 `setMapStyleV2` 实现，无需登录控制台（保留遮罩兜底），原"人工项"作废 |

---

# 第 7 部分 · 执行状态（2026-09-25 收尾）

**进度：105 / 110 已勾选**，未完成 5 项及原因：

| ID      | 状态        | 原因                                                                                     |
| ------- | --------- | -------------------------------------------------------------------------------------- |
| ENG-01  | 未完成（受环境限制） | 并发用例需真实 DB / Testcontainers；本地仅有桩模拟，且环境无法拉起 Docker 数据库。已具备落地路径（见下）                     |
| ENG-03  | 未完成（受环境限制） | 同上：真实事务提交/回滚断言需真实 DB                                                                |
| BE-03   | 部分完成      | 已完成 3/4/5 之外的可做项（删默认账号密码、`.env.example` 模板）；**保留仓库跟踪 `.env*`**——前端 Vite 构建依赖其中 `VITE_*` 变量，移除会直接打断部署链路；CI 门禁保持 `yudao-ui/` 豁免并已注明理由 |
| BE-10   | 评估后暂缓     | 清单原文即备注"影响面较大，建议先与业务确认区域→站点归属模型再动"；`@DataPermission` 使用数仍为 0，需人工确认权限维度后方可实施 |
| SMS-03  | 人工任务      | 短信服务商签名/模板报备需企业资质                                                            |

**ENG-01 / ENG-03 落地路径（交给具备 DB 环境的一轮执行）**
1. `yudao-module-transport/pom.xml` 加 `org.testcontainers:mysql` + `junit-jupiter`（test scope）
2. 新增 `AbstractConcurrencyIT`：`@Testcontainers` 拉起 MySQL 8，`@DynamicPropertySource` 注入数据源
3. 用例（≥6）：①并发抢单同一订单仅 1 成功 ②并发装车超载仅 1 成功 ③并发取消补库存不虚增
   ④手工派单与智能派单同时占用同一车辆仅 1 成功 ⑤同班次并发核销取件码 ⑥并发下架与下单竞态
4. Eng-03：`DispatchServiceImplTest:202/231/252` 的 `verify(mapper)` 改为真实事务断言——注入
   `TransactionTemplate` 后断言"异常后库中无残留行"，并额外加一条"把 `@Transactional` 传播行为改错即失败"的负向守卫用例

---

# 附录 · 关键数字速查（供答辩/汇报引用）

> 数值已按 2026-09-25 收尾状态更新（括号内为改造前基线）。

| 维度      | 数字                                                                                     |
| ------- | -------------------------------------------------------------------------------------- |
| 三端配色    | 主色统一 `#1F5E9E` 山泉蓝（小程序蓝主题 / Web `elColorPrimary` / 大屏 `#4F93D6` 亮调同源）               |
| 算法阻塞风险  | 单请求最坏 ≈**93s**（连接 2s + 读 8s + 轮询预算 15s + 退避 500ms；基线 **≈233s**）                        |
| DB 连接池  | `max-active` **50**（基线 20）                                                             |
| Nginx 超时 | `proxy_read_timeout` **120s**（基线 60s，与后端长任务对齐）                                        |
| 硬编码颜色   | 小程序 **119** 处（基线 353，其中 43 处在 `app.wxss` 令牌定义）/ Web transport 视图 **0 处新增**（CI 门禁拦截） |
| 小字号     | 小程序 `<28rpx` **0** 处（基线 233）                                                         |
| 非令牌圆角   | 小程序数字 `border-radius` **0** 处（基线 173，全部走令牌）                                          |
| 保护能力使用率 | `@RateLimiter` **+3**（匿名算法接口 60s/20 次 IP 维度）；Druid 控制台 `enabled: false`；Actuator 收敛为 `health,info` |
| 端到端契约   | 157 个 HTTP 端点；DO↔DDL 缺列 0；小程序接口命中 68/68；删除幽灵接口 `getTransportModuleStatus`        |
| 测试      | 后端 transport **305 用例全绿** + security 鉴权边界 **6 例**；前端 vitest **40 例**（3 文件）；小程序 **13/13** |
| 本次任务    | **110 项**：已勾 **105**（P0 14/14 全部完成）· 未完成 5（2 项受环境限制、1 项部分、1 项暂缓、1 项人工）              |


---

*本文档由三份审计/诊断报告融合去重生成，已按 v2 山泉蓝配色决策统一口径。执行时若有与本文档冲突的旧报告内容，以本文档为准。*
