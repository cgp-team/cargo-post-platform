# 部署前必读 · 本轮改动说明与风险提示（2026-09-25）

> 面向：负责服务器部署的队友
> 背景：本轮按 `修改清单_统一执行版_2026-09-25.md` 完成 105/110 项修复与改造，覆盖后端、Web 管理端、微信小程序、算法端对接与 CI。
> **本次改动包含数据库结构变更（新增索引/唯一键 + 一次存量数据语义翻转），请务必按下方顺序执行。**

---

## 一、部署前必做（漏做会出问题）

### 1. 增量 SQL —— 必须按序执行，V026 只能执行一次

| 顺序 | 脚本 | 作用 | 注意 |
| --- | --- | --- | --- |
| 1 | `sql/incremental/V024__key_indexes_and_unique_keys.sql` | 补关键索引 + 唯一键（修复多租户关闭后 `(tenant_id, ...)` 左前缀失效导致的全表扫描） | 幂等，含"清理重复行保留最小 id"语句，**先在测试库跑一遍** |
| 2 | `sql/incremental/V025__leg_conflict_indexes.sql` | 联运冲突检测索引 | 幂等 |
| 3 | `sql/incremental/V026__product_status_flip.sql` | **商品上下架枚举语义翻转**（0=上架 → **1=上架**） | ⚠️ **不可重复执行**（`SET status = 1 - status` 翻转两次会还原）。全新环境**不要执行**，直接用最新 `sql/mysql/transport-schema.sql`（已含新语义与 `DEFAULT 1`） |

> V026 是破坏性数据语义变更，前后端已同步改完（后端 `ProductMapper` 两处查询条件、`ProductOrderServiceImpl` 下架校验、3 个 VO 注释、前端筛选/表单/标签）。**如果只更新代码不执行 V026（或在已有库执行），商品上下架状态会整体反掉。**

### 2. 后端配置变更（已在代码里，无需手工改，但要知道影响）

| 配置 | 变更 | 影响 |
| --- | --- | --- |
| Druid `max-active` | 20 → **50** | 需确认 MySQL `max_connections` 余量 ≥ 50 × 实例数 |
| 算法客户端超时链 | 连接 2s / 读 8s / 轮询预算 15s / 退避 500ms | 单请求最坏从 ≈233s 降到 **≈93s**；算法端若响应慢会被更快判失败，属预期 |
| Actuator 暴露 | `*` → **`health,info`** | 监控若抓取 `/actuator/prometheus` 等端点会 404，需同步调整采集配置 |
| Druid 控制台 | `enabled: false` | 需要排查时临时开启，并配强口令 + IP 白名单 |
| 匿名算法接口 | 新增 `@RateLimiter`（60s/20 次，按客户端 IP） | 正常前端调用不受影响；压测或爬虫类调用会收到限流 |

### 3. Nginx 变更

`deploy/nginx/nginx.conf`：`proxy_read_timeout` 60s → **120s**（两处 location），与后端长任务预算对齐。部署时**必须一起更新 nginx 配置并 reload**，否则长耗时的算法类请求仍会在 60s 被切断。

### 4. 短信验证码收不到（SMS-01/02/04）

- 已加限流排障注释与短信日志页「失败原因」列，便于定位失败码；
- **`SMS-03` 仍需人工完成**：真实短信服务商签名与模板报备（需企业资质，约 1~2 工作日）；
- 另请停用/清理 `DEBUG_DING_TALK`（channel id=4）演示渠道——其 WebHook 曾返回成功，测试验证码可能被推送到第三方钉钉群。

### 5. 前端构建

- `yudao-ui/yudao-ui-admin-vue3/.env*` **仍在仓库中跟踪**（有意保留）：Vite 构建依赖其中的 `VITE_*` 变量，移除会直接打断构建链路。CI 的 `.env` 拦截门禁对 `yudao-ui/` 做了显式豁免。
- 新增 `vitest` 依赖并更新了 `pnpm-lock.yaml`，**CI 用 `--frozen-lockfile`，本地请用 `corepack pnpm install`（勿用 npm/yarn）**。
- 构建命令不变：`corepack pnpm build:prod`。

---

## 二、改了什么（按端）

### 后端（`yudao-module-transport` / `-member` / `-framework`）

- **安全**：订单更新接口禁止直写 `status`（状态机守卫）；手工派单并入 `busyVehicleIds()` 在途占用校验（修车辆双派）；`station_id` 越权读**未修**（见第三节）；Druid 控制台关闭；Actuator 收敛。
- **并发一致性**：订单状态机改 CAS、装车容量"读-比-自增"改 CAS、商城取消加 CAS + 状态机（修库存虚增）；39 处 `@Transactional` 长事务拆解（`TransactionAfterCommit` 工具类新增）。
- **算法对接**：`AlgorithmClient` 超时链收敛、`AlgorithmProperties` 参数外置、异常语义统一。
- **新增**：`controller/admin/bigscreen/BigScreenController`（大屏聚合接口 + 服务端缓存 48s）、`DashboardController` 订单趋势新增 `granularity=hour`（24 点）、`TraceIdFilter`（链路追踪）。
- **测试**：transport 模块 305 例全绿；新增 `OrderStatusTransitionMatrixTest`（状态流转矩阵）；`yudao-spring-boot-starter-security` 补 6 个鉴权边界用例（原目录为空）。

### Web 管理端（`yudao-ui/yudao-ui-admin-vue3`）

- **视觉令牌**：新增 `styles/tokens.scss`；主色统一山泉蓝 `#1F5E9E`；表头/文本色改 Element 令牌（深色模式可跟随）。
- **交互修复**：`Dialog` 组件 `closeOnClickModal` 默认 false（此前硬编码 true 且覆盖使用方传参，点遮罩会丢表单数据）；19 张表补空态插槽；57 处 catch 分四类处理（轮询类加 `silent`，弱网不再弹窗轰炸）。
- **口径统一**：`views/transport/constants.ts` 为枚举单一来源（修正此前把车辆"静态状态"与"实时运营状态"两字段语义混写的错误）；商品/状态术语统一（场站→站点、转接站→换乘站、智能调度→智能派单）。
- **大屏**：新增 `views/transport/bigscreen/index.vue`（1920×1080 等比缩放）+ 暗色底图（百度 GL `setMapStyleV2`，不支持时遮罩兜底）。
- **地图**：坐标转换唯一来源 `components/Map/src/utils.ts`（删 3 处重复实现 + 259 行死代码）；新增网格聚合（>40 点启用，避免 500+ 覆盖物卡顿）。
- **新增测试**：vitest 接入（`pnpm test`，当前 **40 例**，覆盖枚举契约、树工具、地图工具）。

### 微信小程序（`cargo-post-miniprogram`）

- **启动链路**：首页内嵌闪屏遮罩（最短 800ms / 封顶 2500ms / 300ms 淡出）+ 启动路由决策（**冷启动不再硬跳登录页**）；`app.json` 首页由 `login` 改为 `index`。
- **视觉**：字号 `<28rpx` 从 233 处清零；数字圆角 173 处全部令牌化；硬编码色从 353 处降至 119 处（其中 43 处在 `app.wxss` 令牌定义内）。
- **交互**：75+ 处 `wx.showToast` 统一走 `utils/toast.js`；空态/错误态统一 `empty-state` 组件；点击区按适老化基线放大。

---

## 三、已知问题与遗留项（请注意，不是"改完了没问题"）

| 项 | 状态 | 说明 |
| --- | --- | --- |
| **ENG-01 / ENG-03** | ❌ 未完成 | 抢单/装车/取消的**真实并发测试**与事务回滚真实验证，需要真实 DB 或 Testcontainers 环境（本地无法拉起 Docker 数据库）。当前这些路径只有单元测试桩验证，**建议在预发环境做一次真实并发压测**（同一订单并发抢单、超载装车、并发取消补库存）。 |
| **BE-10 数据权限** | ⚠️ 暂缓 | 任何持有 `transport:order:query` 的账号仍可读全平台订单（含收件人姓名/手机号/取件码）。落地需业务先确认"区域→站点"权限维度，否则会误伤。 |
| **BE-03 凭据治理** | ⚠️ 部分完成 | 已删除前端默认登录账号密码、补 `.env.example`；但 `.env*` 仍被 git 跟踪（构建依赖）。**已泄露的凭据（admin 密码、百度地图 AK、AES 密钥、DB 口令）必须人工轮换**——它们已进入 Git 历史，`git rm --cached` 无法消除历史泄露。 |
| **多租户关闭** | ⚠️ 设计现状 | `application.yaml` 中 `tenant.enable: false`，因此 `(tenant_id, ...)` 复合索引左前缀全部失效——这正是 V024 要补独立索引的原因。是否启用多租户需产品决策。 |
| **临时产物清理** | ⚠️ 待办 | 工作区仍有约 130 个未跟踪的临时产物/误创建文件（`test_*.txt`、`artifacts/_patch_*.py`、`logs/` 等）。已在 `.gitignore` 中加忽略规则防止误入库，但**建议本地清理**，清单见第四节。 |
| **小程序残余硬编码色** | 🟡 持续治理 | 残余 76 处（分布在 12 个页面 wxss，多为 SVG data-uri 内颜色与品牌点缀色），不阻断发布。 |

### 新增的 CI 门禁（会让旧写法在 PR 上变红，属预期）

`.github/workflows/ci.yml` 增加：
1. **前端单测门禁** `pnpm test`（vitest，40 例，失败即红）；
2. **transport 视图 ESLint 门禁**（当前全绿，新增违规即红）；
3. **硬编码颜色门禁** `scripts/scan_hardcoded_colors.py`：检查 **git diff 新增行**的 hex 字面量（白名单：`plugins/echarts`、`components/Map`、SVG 属性）。当前为**仅上报**模式（`|| true`），确认稳定后可改为硬门禁；
4. **Java 静态扫描** `scripts/scan_java_issues.py`（同样先仅上报）；
5. triage：同分支连续 push 自动取消旧 job（省 runner）。

> 若 CI 失败：**先看是哪一项门禁红的**。颜色/ESLint 门禁只检查新增行，历史存量不拦。

---

## 四、本地文件说明（哪些没进仓库）

以下为本地存在但**未提交**的文件，供你判断是否需要：

- **交付文档（建议保留在本地/按需入库）**：`修改清单_统一执行版_2026-09-25.md`（唯一事实来源，含执行状态）、`执行进度报告_2026-09-25.md`、`审计报告_全景_2026-09-25.md`、`UI审计与统一设计系统方案_2026-09-25.md`、`诊断_短信验证码收不到_2026-09-25.md`。
- **临时产物（建议删除）**：`_t_q1~3/`、`ab_out.txt`、`test_feasibility*.txt`、`test_output.txt`、`full_test_results.txt`、`more_test_results.txt`、`scale_train.err`、`train100.err`、`train800b.err`、`artifacts/_patch_*.py` 等、`logs/`、`css/`、`js/`、`miniprogram/`（根目录下这三个疑似误创建目录）。
- **误创建文件（建议删除）**：`-w`、`代码版本：当前`、`日期：2026-08-26`、`审计日期：2026-08-26`（shell 重定向失误产生的文件名）。以上已在 `.gitignore` 加忽略规则。
- **算法端大文件**：`algorithm.zip`、`learning.zip`、`algorithm/data/*.pkl`、`algorithm/data/model_registry/`、`tools/osm-data/`、`tools/graphhopper/` —— 体积大，未入库，按需自行分发。

---

## 五、验证命令（部署后自检）

```bash
P=/c/Users/袁/Desktop/测试/cargo-post-platform
MP=/c/Users/袁/Desktop/测试/cargo-post-miniprogram

# 后端：transport 模块单测（预期全绿）
/c/Users/袁/apache-maven-3.9.16/bin/mvn.cmd -B -pl yudao-server -am test

# 前端：类型检查 + 单测 + 构建
cd $P/yudao-ui/yudao-ui-admin-vue3 && corepack pnpm ts:check && corepack pnpm test && corepack pnpm build:prod

# 小程序：纯 Node 测试套件（13 个文件，预期 13/13）
for f in $MP/tests/*.test.js; do node "$f" || echo "FAIL: $f"; done

# 运行时自检
curl -s -o /dev/null -w "%{http_code}\n" http://<域名>/actuator/env        # 预期 404（已收敛）
curl -s -o /dev/null -w "%{http_code}\n" http://<域名>/druid/index.html   # 预期 404（控制台已关）
```

## 六、回滚

- 代码：`git revert <merge commit>` 或切回上一个 release tag；
- 数据库：**V024/V025 只新增索引，回滚时可直接 drop 对应索引**（脚本内置 `information_schema` 存在性判断，可重复执行）；**V026 回滚需反向执行一次同样的翻转语句**（`UPDATE transport_product SET status = 1 - status WHERE deleted = 0`），并同时回滚代码，否则前后端语义不一致。
