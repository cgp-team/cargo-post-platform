-- ============================================================================
-- 346 路主线演示数据 —— 已并入 sql/mysql/demo-real-orders.sql
-- ============================================================================
--
-- 为什么不再单独维护本文件：本文件不在部署迁移清单（deploy-dev.yml）里，单独放着的话
-- 每次部署都不会生成这批演示订单，演示时「调度中心 → 订单池」看不到它们。
--
-- 现在这批数据（重庆工商大学站、320 路站序补站、346/347/303/318 等车辆与班次绑定、
-- 订单 A~E 与重邮→重庆交通大学、重庆大学A区订单、时间窗规范化）统一放在
--     sql/mysql/demo-real-orders.sql
-- 末尾的「346 路主线演示数据」区块里 —— 该文件在部署迁移清单中，部署即自动执行（幂等）。
--
-- 需要单独补数据时，直接执行：
--     mysql ... < sql/mysql/demo-real-orders.sql
-- 即可（可重复执行）。