[pytest]
pythonpath = .
testpaths = tests
# 标记注册（消除 PytestUnknownMarkWarning，并支持 CI 分层：-m "not slow" 跳过长时搜索基准）
markers =
    slow: 长时搜索/基准用例（HACO-CPS 多场景搜索、稳定性与消融重跑），本地与 PR 快速反馈时可用 -m "not slow" 跳过
# 默认输出最慢的 15 个用例，便于持续优化 CI 时长
addopts = --durations=15
